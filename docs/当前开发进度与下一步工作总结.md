# 当前开发进度与下一步工作总结

生成时间：2026-06-13

## 1. 本次代码检查结果

本总结在检查当前代码后编写，检查结果如下：

| 检查项 | 结果 |
| --- | --- |
| `python manage.py check` | 通过，`System check identified no issues` |
| `python manage.py check --deploy` | 模拟生产 HTTPS 配置通过，`System check identified no issues` |
| `python manage.py test` | 通过，762 个测试全部 OK，SQLite 环境下 3 个 PostgreSQL 行级锁并发用例按预期跳过 |
| `python manage.py makemigrations --check --dry-run` | 通过，`No changes detected` |
| `python -m pip check` | 通过，`No broken requirements found` |
| `python manage.py check_url_references` | 通过，715 个静态 URL 引用、712 个 URL 名称和 294 个模板参数引用均一致 |
| `python manage.py check_templates` | 通过，113 个模板均可编译 |
| `python manage.py check_permissions` | 通过，17 个默认权限记录齐全，代码权限码与默认权限清单一致 |
| `python manage.py check_permission_references` | 通过，15 个静态权限引用均已登记为默认权限 |
| `python manage.py check_route_protection` | 通过，305 个业务 URL 均有登录或权限保护 |
| `python manage.py check_csrf_tokens` | 通过，108 个 POST 表单均包含 `csrf_token` |
| `python manage.py check_navigation_pages` | 通过，51 个主导航页面可正常打开 |
| `python manage.py check_low_frequency_entrypoints` | 通过，91 个导入、导出、打印低频入口可反转，其中 44 个导入页或导入模板入口可访问 |
| `python manage.py business_smoke_test` | 通过，6 个检查点全部 OK，默认回滚临时数据 |
| `python manage.py release_gate --include-tests --report-file docs\latest-release-gate-report.md` | 最新全量开发机门禁通过，13 个步骤全部 OK；归档报告显示 `Ran 762 tests in 441.681s` |
| `python manage.py release_gate --report-file docs\latest-release-gate-report.md` | 快速开发机门禁命令可用；当前默认门禁为 12 个步骤，已包含低频入口烟测 |
| `python manage.py prelaunch_report --release-version 2026.06.11.local --bootstrap-username admin --deployment-runbook-file docs\deployment-runbook-2026.06.11.local.md --report-file docs\prelaunch-acceptance-report.md` | 已生成本地预上线验收报告；部署命令清单、初始化管理员、静态文件、备份、备份校验、恢复演练、事务后事件处理和发布记录均已通过，本地报告剩余 0 个失败和 2 个预警，均为生产环境标记、生产安全检查和生产严格预检等服务器实机项 |
| 数据库迁移状态 | 所有 app 迁移均为 `[X]` 已应用，本轮新增 `accounts.0011`，并已应用此前 `accounts.0010`、`accounts.0009` 和 `inventory.0002` |
| 模板残留检查 | 已清理主要 `Create your ... here` 模板注释 |
| 当前路由 | 已接入业务工作台、登录退出、各模块基础列表页和 Django Admin |

当前项目已是 Git 仓库，本轮变更可通过 `git status` 和 `git diff` 继续审阅。

补充说明：本地开发库直接执行 `python manage.py production_preflight --strict` 仍会按预期失败，因为当前不是 `DJANGO_ENV=production`，且正式上线完整门禁必须在生产配置和 PostgreSQL 环境下执行。当前本地已完成 `bootstrap_admin` 验收和 `collectstatic`，正式上线时应按 [生产上线部署与运维手册.md](生产上线部署与运维手册.md) 完成生产环境变量、PostgreSQL、附件扫描命令或风险接受人配置后再执行严格预检。

本轮最新同步：

- 角色模块隔离已补强：新增 `sales.view`、`purchase.view`、`inventory.view`、`production.view` 和 `bom.view` 模块查看权限；销售、采购、库存、生产、BOM 的列表、详情、导出、打印和主导航均按“查看权限或处理权限”控制，处理动作仍要求对应 `*.process` 权限。
- 销售角色只勾选“处理销售单据”时，主导航和直接访问 URL 都只开放销售相关页面；采购角色可在采购侧查看欠料提醒并生成采购需求；老板类只读角色可通过模块查看权限进入多个模块但看不到权限管理入口。
- 财务入口继续按金额敏感口径控制：财务列表、详情、导出和打印要求 `finance.view_amount`，收付款确认、红冲、待处理余额处理等动作额外要求 `finance.payment_process`。
- 已重新生成 `docs/latest-release-gate-report.md`，最新发布前门禁 13 步全部通过；完整自动测试为 762 个用例。
- 已重新构建 [ERP安装包](../ERP安装包) 和 [ERP安装包.zip](../ERP安装包.zip)，安装包内包含一键安装手册、NSSM、离线 Python 依赖轮子和最新权限迁移，排除了 `tests_safety`、开发期大安装包目录、建设计划文档和未确认的 `.docx` 资料。
- 安装包构建脚本已改为 Windows PowerShell 兼容写法，中文目录和中文文件名在运行时拼接，避免无 BOM UTF-8 脚本在 Windows 10 默认 PowerShell 下解析失败。
- 审批加签已完成：审批人可将审批临时加签给其他用户，加签人同意后审批会返回原审批人继续处理。
- `approvals.Approval` 已新增 `return_to_approver` 字段，并生成 `approvals.0002` 迁移。
- 审批详情页已提供加签表单，并展示加签后返回审批人。
- 审批动作日志已记录加签、加签同意后的返回处理、IP 和 User-Agent。
- 财务对账单已新增固定明细快照表 `finance.ReconciliationItem`，并生成 `finance.0002` 迁移。
- 对账单确认时会固化销售订单或进货单的未结明细；已确认后的详情和打印优先读取快照，避免来源单据后续变化导致历史对账单漂移。
- 已新增 [生产上线部署与运维手册.md](生产上线部署与运维手册.md)，覆盖生产环境变量、PostgreSQL、迁移、静态文件、Nginx、定时任务、备份恢复演练和上线前检查清单。
- `.env.example` 已补充 `ERP_BACKUP_DIR`、`DJANGO_LOG_DIR` 和 `DJANGO_SESSION_COOKIE_AGE`，`settings.py` 已显式读取 `ERP_BACKUP_DIR`。
- 销售订单已新增 CSV 导入模板和导入页面；导入按订单号合并多行明细，只生成草稿订单，后续仍走提交审核、库存检查和确认流程。
- 采购需求和采购单已新增 CSV 导入模板和导入页面；导入按单号合并多行明细，只生成草稿单据，后续仍走审核、采购到货和库存入库流程。
- 进货单已新增 CSV 导入模板和导入页面；导入按进货单号合并明细，只生成待入库进货单，不生成批次、不回写采购单到货数量，后续仍走确认入库和二次验证。
- 借样单已新增 CSV 导入模板和导入页面；导入按借样单号合并多行明细，只生成待审核借样单，不自动扣库存，后续仍走确认借样出库和二次验证。
- 客户退货已新增 CSV 导入模板和导入页面；导入按退货单号合并明细，只生成草稿客户退货单，不自动入库或调整应收。
- 销售出库已新增 CSV 导入模板和导入页面；导入按出库单号合并明细，只生成待确认出库单，不自动扣库存，后续仍走确认出库和二次验证。
- 供应商退货已新增 CSV 导入模板和导入页面；导入按退货单号合并明细，只生成草稿供应商退货单，不自动扣库存，后续仍走确认退货出库和二次验证。
- 生产指令已新增 CSV 导入模板和导入页面；导入可关联销售订单行并锁定其 BOM，或按成品默认启用 BOM 生成待生产指令，不自动生成领料或入库单。
- 生产领料已新增 CSV 导入模板和导入页面；导入按领料单号合并明细，只生成待确认领料单，不扣减库存，后续仍走确认领料出库和二次验证。
- 生产入库已新增 CSV 导入模板和导入页面；导入按入库单号合并明细，只生成待确认生产入库单，不生成批次、不更新生产指令已入库数量，后续仍走确认入库和二次验证。
- 客户收款和供应商付款已新增 CSV 导入模板和导入页面；导入只生成待审核收付款单，不自动核销，不生成待处理余额，后续仍走确认核销和二次验证。
- 日志文件轮转已改为 `SafeRotatingFileHandler`，Windows 文件短暂占用导致轮转失败时会继续写当前日志，避免测试或生产控制台刷出轮转异常。
- 附件来源权限继续收紧：采购、库存、生产附件必须具备对应模块处理权限；财务附件必须具备金额查看权限；销售附件继续遵守销售数据范围。
- 附件入口继续补齐：采购需求、生产指令、客户待处理余额、供应商待处理余额和审批详情页已显示附件面板，并加入页面回归测试。
- 生产安全配置已补强：`settings.py` 支持通过环境变量控制 HTTPS 跳转、Secure Cookie、CSRF Secure Cookie 和 HSTS；模拟生产环境开启 preload 后 `python manage.py check --deploy` 无警告。
- 生产数据库保护已补强：`DJANGO_ENV=production` 时 `DB_ENGINE` 必须为 `postgres`，否则应用拒绝启动，避免正式环境误连 SQLite。
- 历史导出下载已加固：导出日志只能下载 `MEDIA_ROOT/exports/<导出号>.csv` 下的 CSV 文件，数据库路径被篡改到外部文件时返回 404。
- 即时导出响应已统一：各模块列表导出不再直接 `open(file_path)`，统一通过 `export_file_response` 复核导出目录、导出号和 CSV 后缀后返回文件。
- 通用 CSV 导出已防 Excel 公式注入：以 `=`、`+`、`-`、`@` 开头或前导空白后接这些字符的文本会按纯文本写入。
- 备份恢复演练已防 zip 路径穿越：恢复演练改为逐项安全解压，拒绝绝对路径、盘符路径和 `..` 路径成员。
- 备份清理已加路径护栏：旧备份清理只会删除 `ERP_BACKUP_DIR` 内的文件，数据库记录被篡改到外部路径时会拒绝删除并保留记录供人工排查。
- 生产预检已补强备份目录隔离：生产模式下 `ERP_BACKUP_DIR` 不能位于应用代码目录内，避免备份随代码发布、误删或被 Web 进程错误暴露。
- 附件下载异常处理已收紧：附件路径异常、文件缺失或 storage 拒绝访问时返回 404，且不会写入虚假下载日志。
- 登录安全已补强：停用、锁定或逻辑删除账号不能再登录系统，登录表单会提示联系系统管理员。
- 会话追踪与强制失效已落地：`UserSessionTrackingMiddleware` 会记录登录用户的 session、IP、User-Agent 和最近访问时间；权限管理员可在 ERP 页面查看登录会话并强制失效，用户下一次请求会被自动退出。
- 普通用户已支持在 ERP 顶部栏自助修改登录密码，旧密码会立即失效。
- 权限管理员已可在 ERP 页面查看用户、角色、权限清单，并维护用户、角色和角色权限。
- 用户/角色维护动作已要求当前密码与操作原因，并写入审计日志；系统会阻止管理员停用自己、删除自己或移除自己的最后一个权限管理角色。
- 权限管理员已可重置用户密码；重置时要求当前密码与操作原因，会写入审计日志，并自动强制失效目标用户的有效登录会话。
- BOM 明细删除已补强为必须输入当前密码和删除原因，并写入审计日志，避免关键生产资料被无痕改动。
- BOM 关键维护动作已补齐审计闭环：BOM 新建、头信息编辑、明细新增、明细编辑、复制为新版本、启用和停用都会写入审计日志；启用和停用还要求当前密码与操作原因。
- 主要业务单据编辑页已补充“修改原因”输入；销售、采购、生产、财务编辑审计日志会记录 `operation_reason`，方便上线后追踪“谁在什么原因下改了单”。
- 基础资料编辑审计已补齐：物料、客户、供应商、客户产品、客户地址、单位换算、供应商价格的编辑都会记录修改原因和前后快照。
- 附件上传已改为后端按来源单据 ID 解析真实单号，不再信任前端提交的 `source_doc_no`，避免附件列表和审计展示被伪造单号污染。
- 后台任务失败告警已补齐：备份校验、恢复演练、事务后事件等后台命令失败时，会生成紧急系统消息通知权限管理员和超级管理员。
- 事务后事件处理已改为失败可见并支持自动重试：`process_pending_events` 只要存在处理失败事件，就会让后台任务记录为失败；失败事件会按 `ERP_PENDING_EVENT_RETRY_BASE_MINUTES` 退避，到期后自动重试，超过 `ERP_PENDING_EVENT_MAX_RETRIES` 后停止排队并保留失败状态供运维处理。
- 附件存储路径已加固：附件登记和下载都只允许 `attachments/` 目录下的合法附件路径，拒绝绝对路径、目录穿越和被篡改到其他目录的文件路径。
- 生产配置护栏已补强：`DJANGO_ENV=production` 时强制拒绝 `DJANGO_DEBUG=true` 和空 `POSTGRES_PASSWORD`，并补充 `DJANGO_CSRF_TRUSTED_ORIGINS` 环境变量配置。
- Django Admin 暴露面已收紧：`/admin/` 只允许启用、未删除、状态正常的超级管理员访问，普通 staff 账号不能进入后台。
- CSV 导入读取已统一加固：所有导入服务共用 `read_csv_dict_rows`，支持 `ERP_MAX_CSV_IMPORT_ROWS` 行数上限，畸形 CSV 和列数超表头会写入导入任务失败明细，不再冒 500。
- 系统健康检查页已补充事务后事件队列概览和发布门禁报告状态，展示待处理、处理中、失败事件数量、最近失败事件错误，以及最近一次 `release_gate` 报告是否通过且未过期，便于预上线运维发现静默积压和过期门禁。
- 主导航回归测试已覆盖用户管理、角色管理、权限清单和登录会话入口，避免后续改导航时遗漏管理入口。
- 首次上线初始化命令已补齐：`python manage.py bootstrap_admin` 可幂等创建或修复初始超级管理员、权限管理员角色和默认权限；重复执行默认不覆盖已有密码，只有显式 `--reset-password` 才重置。
- 首次上线初始化只读验收已补齐：`python manage.py bootstrap_admin --username <用户名> --check-only` 可验证默认权限、权限管理员角色和指定管理员账号已就绪，且不会修改数据库。
- Windows 生产 WSGI 运行方式已落地：`requirements.txt` 已锁定 `waitress==3.0.2`，生产上线手册已给出 `waitress-serve.exe` 启动示例。
- 附件安全扫描已补齐可配置钩子：`ERP_ATTACHMENT_SCAN_COMMAND` 为空时标记“无需扫描”，配置外部扫描命令后返回码非 0、超时或无法执行都会拒绝附件登记；正式上线暂不启用扫描时可填写 `ERP_ATTACHMENT_SCAN_RISK_ACCEPTED_BY`，生产预检和预上线报告会显示风险接受人，避免口头风险接受无证据；附件详情和单据附件面板会展示扫描状态。
- 生产部署手册已和生产配置护栏对齐：`DJANGO_ALLOWED_HOSTS` 示例不再包含会被生产模式拒绝的 `127.0.0.1`。
- 生产上线自动预检命令已补齐：`python manage.py production_preflight --strict` 会检查生产标记、数据库连接和 PostgreSQL 口径、迁移状态、附件/备份目录可写、附件/备份/静态目录隔离、HTTPS 安全配置、静态文件目录、初始超级管理员、权限管理员角色、附件扫描配置、失败后台任务和失败事务后事件。
- 预上线验收清单已新增：[预上线验收清单.md](预上线验收清单.md)，覆盖代码门禁、生产配置、初始化、主业务流程、权限审计、导入导出打印、运维恢复和上线签字。
- 发布记录闭环已补齐：`python manage.py record_release <版本号>` 可登记发布版本、发布人和摘要，权限管理员可在 ERP 页面查看发布记录，生产上线部署手册和预上线验收清单已同步该步骤。
- 业务冒烟测试命令已补齐并扩展生产、借样与财务链路：`python manage.py business_smoke_test --operator <用户名>` 会用临时数据执行销售确认、销售出库、欠料生成采购需求、采购单、采购入库、齐套重检、生产领料、生产入库、借样出库、借样归还、借样转销售、客户收款核销、供应商付款核销、待处理余额关闭和红冲反向核销，默认事务回滚，适合每次上线前重复执行。
- 发布前门禁命令已补齐：`python manage.py release_gate --operator <用户名>` 默认串联 Django 系统检查、URL 引用完整性检查、模板语法检查、权限配置检查、权限引用完整性检查、路由保护检查、CSRF 表单检查、导航页面烟测、低频入口烟测、迁移一致性、Python 依赖检查和业务冒烟测试；需要时可加 `--include-deploy-check` 执行 Django 生产安全检查，加 `--include-tests` 执行全量测试，加 `--include-production-preflight` 执行严格生产预检，加 `--report-file docs\latest-release-gate-report.md` 生成可归档的 Markdown 门禁报告。生产预检和健康检查页会读取 `ERP_RELEASE_GATE_REPORT_FILE`，并按 `ERP_RELEASE_GATE_MAX_AGE_HOURS` 判断报告是否过期。
- 预上线验收报告已区分开发机普通门禁和正式上线完整门禁：正式上线要求最近门禁报告包含 15 个必备步骤，即 Django 系统检查、生产安全检查、URL 引用完整性检查、模板语法检查、权限配置检查、权限引用完整性检查、路由保护检查、CSRF 表单检查、导航页面烟测、低频入口烟测、迁移一致性检查、Python 依赖检查、业务冒烟测试、完整自动测试和生产严格预检；本地当前 `latest-release-gate-report.md` 是 13 步全量开发机门禁，缺少生产安全检查和生产严格预检，所以预上线报告会保留一条“非完整上线门禁”预警并列出缺失步骤。
- 预上线验收报告命令已补齐：`python manage.py prelaunch_report --report-file docs/prelaunch-acceptance-report.md` 会汇总发布门禁、初始化验收、生产预检、成功备份、备份校验、恢复演练、事务后事件处理、后台任务和发布记录；加 `--strict` 时存在失败或预警会返回非 0，适合正式上线脚本，`--bootstrap-username` 可指定初始化管理员账号，`--release-version` 可强制校验本次发布记录。
- 预上线验收报告已继续补强运维证据门禁：没有成功备份、成功备份校验、成功恢复演练、最近事务后事件处理或发布记录时会生成预警；成功备份和备份校验默认要求 24 小时内，恢复演练默认要求 168 小时内，事务后事件处理默认要求 30 分钟内；正式上线前需要 `backup_daily`、`verify_backups`、`restore_drill`、`process_pending_events` 和 `record_release` 都留下可归档证据。
- 生产配置模拟命令已补齐：`python manage.py simulate_production_settings --host <正式域名>` 会在子进程中设置生产安全环境变量并执行 `check --deploy --fail-level WARNING`，用于开发机提前验证生产配置组合，不连接生产数据库。
- 部署命令清单已补齐：`python manage.py deployment_runbook --host <正式域名> --operator <用户名> --release-version <版本号>` 会按推荐顺序输出迁移、静态文件、初始化管理员创建与验收、发布门禁、备份、恢复演练、业务冒烟、发布记录和预上线报告命令；可加 `--output-file docs/deployment-runbook-<版本号>.md` 直接生成可归档文件。
- 部署命令清单与预上线报告已形成闭环：`deployment_runbook --output-file ...` 生成的最后一步会自动携带 `--deployment-runbook-file`，`prelaunch_report` 会校验清单标题、预上线命令和本次发布版本号，本地样例清单已归档到 [deployment-runbook-2026.06.11.local.md](deployment-runbook-2026.06.11.local.md)。
- 本轮补齐 `pending_events` 到期重试语义：失败事件保留失败状态和错误原因，同时写入 `next_retry_at`；后台任务只领取待处理或到期重试事件，成功后清空错误和重试时间，超过最大重试次数后不再自动重试。
- 本轮补齐 `running` 事务后事件超时回收：超过 `ERP_PENDING_EVENT_RUNNING_TIMEOUT_MINUTES` 仍处于处理中的事件会在下一次 `process_pending_events` 中重新领取；健康检查页和生产预检会提示超时处理中事件数量。
- 本轮补齐安全与可靠性回归测试：`tests_safety` 已覆盖认证、权限、CSRF、XSS、SQL 注入、库存一致性、BOM 计算、单位换算、核销越界和并发入口；SQLite 不支持行级锁的 3 个并发用例会跳过，正式并发语义由 PostgreSQL 验证。
- 本轮补强核销最后防线：客户收款核销明细和供应商付款核销明细即使绕过服务层直接 `objects.create()`，也会在 `save()` 中锁定目标订单/进货单/对账单并校验剩余可核销金额，避免管理后台或脚本误写超额核销。
- 本轮补强库存扣减公共函数：`deduct_batch_inventory()` 在调用方未开启事务时会自动开启 `transaction.atomic()`，保持独立调用和服务层调用的锁定语义一致。
- 本轮补齐 `running` 后台任务超时回收：启动同类型后台任务前会把超过 `ERP_BACKGROUND_JOB_RUNNING_TIMEOUT_MINUTES` 的旧运行中任务标记失败并通知管理员，避免进程异常退出后永久阻塞备份、恢复演练或事务后事件处理。
- 本轮补强生产预检日志目录校验：`production_preflight` 会检查日志目录可写，并校验附件、备份、日志和静态文件目录互相隔离，避免日志目录误放在静态或附件目录内。
- 本轮优化预上线报告结论说明：非生产环境报告会按实际失败/预警项列出剩余待处理内容，当前本地报告仅剩“完整上线门禁”和“生产环境标记”两个生产实机项。
- 本轮修正生产上线手册中的 `release_gate` 示例命令，统一使用 `--include-deploy-check --include-tests --include-production-preflight`，避免按手册执行时生成非完整上线门禁报告。
- 本轮新增 URL 引用完整性检查：`python manage.py check_url_references` 会扫描模板 `{% url %}`、Python `reverse/redirect` 等静态 URL name，并对模板里的静态 URL 参数做反向解析校验，当前 678 个静态引用、682 个 URL 名称和 279 个模板参数引用全部可解析。
- 本轮新增模板语法检查：`python manage.py check_templates` 会编译检查 `templates/` 下 105 个 Django 模板，防止低频页面模板语法错误延迟到生产暴露。
- 本轮新增权限配置检查：`python manage.py check_permissions` 会校验代码权限码、默认权限清单和数据库默认权限记录一致，当前 17 个默认权限齐全。
- 本轮新增权限引用完整性检查：`python manage.py check_permission_references` 会扫描代码和模板中的静态权限码引用，防止页面、表单或后端误用未登记权限码。
- 本轮新增路由保护检查：`python manage.py check_route_protection` 会从 URLConf 扫描业务 URL，确保除登录/退出和 Django Admin 外都继承登录或 ERP 权限保护。
- 本轮新增 CSRF 表单检查：`python manage.py check_csrf_tokens` 会扫描模板中的 POST 表单，确保都包含 `csrf_token`。
- 本轮新增导航页面烟测：`python manage.py check_navigation_pages` 会登录管理员并逐个打开主导航页面，确保导航入口不返回 403、404 或 500。
- 本轮新增低频入口烟测：`python manage.py check_low_frequency_entrypoints` 会检查导入、导出和打印入口可反转，并实际访问导入页和导入模板入口；导出和对象级打印只做反转检查，避免门禁产生业务导出日志或依赖大量样本单据。
- 本轮发布门禁已接入 URL 引用完整性检查、模板语法检查、权限配置检查、权限引用完整性检查、路由保护检查、CSRF 表单检查、导航页面烟测和低频入口烟测；正式上线完整门禁要求 15 个必备步骤齐全，预上线报告会列出缺失步骤而不是只按步骤数判断。
- 本轮补强事务后事件可观察性：`sample_out`、`sample_returned`、`sample_to_sales`、`customer_return_in`、`supplier_return_out`、`location_transfer`、`production_material_issued`、`production_received`、`stock_count_adjusted`、`purchase_request_created`、`purchase_order_created`、`payment_confirmed` 和 `payment_reversed` 已从通用事件提示升级为专用处理器，系统消息会挂到对应业务单据号，方便操作者和运维追踪。
- 本轮统一 BOM 物料需求计算口径：新增 `bom.services.required_component_qty_base`，销售齐套检查和生产指令生成领料单都按 `BOM 用量 / BOM 基准数量 * 生产数量 * (1 + 损耗率)` 计算，并执行单位换算和物料数量精度向上取整；生产生成领料单遇到缺失单位换算时会给出明确错误提示。
- 本轮修正采购入库后的欠料重检边界：`confirm_purchase_receipt` 只在入库事务内写库存、采购单据状态和 `purchase_received` 事务后事件，不再同步重检销售欠料；`process_pending_events(event_type="purchase_received")` 统一执行欠料重检和齐套消息，避免入库事务持锁过长及重复副作用。业务冒烟测试和页面回归测试已同步为“先事件入队、后事件处理齐套”的语义。
- 本轮补齐生产友好的异常页：新增 `handler400` 和 `handler500`，并将 400、403、404、500 错误页统一到共用模板，避免生产环境出现默认错误页；系统错误页测试和快速发布门禁已通过。
- 本轮优化通用列表空状态：所有继承 `ErpListView` 的列表会区分“暂无数据”和“筛选无结果”，筛选无结果时提供清除筛选入口；物料列表已补回归测试，快速发布门禁已通过。
- 本轮收紧生产路径配置口径：`DJANGO_STATIC_ROOT`、`ERP_ATTACHMENT_DIR`、`ERP_BACKUP_DIR`、`DJANGO_LOG_DIR` 和 `ERP_RELEASE_GATE_REPORT_FILE` 支持绝对路径；填写相对路径时统一解析到项目 `BASE_DIR` 下，避免 Windows 服务、计划任务或 Linux systemd 从不同工作目录启动后路径漂移。`.env.example` 已补充静态目录、日志级别、登录跳转和初始化管理员密码变量。
- 本轮同步生产上线手册环境变量示例：Windows 和 Linux 部署示例已补充 `DJANGO_STATIC_ROOT`、`DJANGO_LOG_LEVEL`、`DJANGO_LOGIN_URL`、`DJANGO_LOGIN_REDIRECT_URL` 和 `ERP_BOOTSTRAP_ADMIN_PASSWORD`，并说明路径类变量的绝对/相对路径解析规则。
- 本轮补强生产预检目录隔离：附件、静态文件、备份和日志目录现在做两两互斥检查，能发现静态目录误放进附件目录、附件目录误放进静态目录等反向嵌套风险，避免 Nginx/静态目录配置不当暴露附件或日志。
- 本轮新增通用列表排序：`ErpListView` 支持白名单排序字段，排序链接会保留当前筛选条件；第一批已覆盖物料、销售订单、欠料、销售出库、采购需求、采购单、进货单、库存批次、库存流水、生产指令、客户收款和供应商付款等高频列表。
- 本轮同步 CSV 导出排序：主数据、销售、采购、库存、生产和财务导出基类会复用列表页白名单排序；用户在列表页按某列升序/降序后点击导出，CSV 顺序会与当前列表一致，并在导出日志中记录实际排序口径。

### 1.1 本轮复核修复结果

针对最新一轮 Model 与配置检查，本轮已完成以下修复并生成增量迁移：

| 问题 | 处理结果 |
| --- | --- |
| `bom.models.Bom` 缺少 `base_qty`、`effective_date`、`expiry_date`、`is_default` | 已补齐字段，并生成 `bom.0002` 迁移 |
| `accounts.User.SecurityLevel` 文案与设计文档不一致 | 已调整为 `L2=敏感账号`、`L3=部门管理员`，并生成 `accounts.0002` 迁移 |
| `masterdata.CustomerAddress` 缺少地址类型 | 已新增 `address_type`，支持收货、退货、样品地址，并生成 `masterdata.0002` 迁移 |
| `sales.SalesOrder` 缺少审核人和审核时间 | 已新增 `approved_by`、`approved_at`，销售订单确认服务会自动写入 |
| `sales.ShortageAlert.purchase_request_id` 不是外键 | 已改为 `purchase_request` 外键，数据库列仍保留 `purchase_request_id`，避免迁移时丢失旧值 |
| `config/settings.py` 生产安全配置偏弱 | 已调整 `SECRET_KEY` 生产强制环境变量、`DEBUG` 默认关闭、`LOGIN_URL=/login/`、Session 8 小时、基础日志输出，并新增 HTTPS 跳转、Secure Cookie、CSRF Secure Cookie、HSTS 环境变量和生产环境 PostgreSQL 强制校验 |

本轮新增的测试断言：

- 销售订单确认后会写入 `approved_by` 和 `approved_at`。
- 欠料生成采购需求后，`ShortageAlert.purchase_request` 外键关系可正常使用。

### 1.2 本轮页面骨架、权限与事件处理补充

本轮继续完成以下开发：

| 项目 | 处理结果 |
| --- | --- |
| `process_pending_events` 事件处理过薄 | 已改为按事件类型分发处理器，`purchase_received` 会执行欠料重检并生成消息，未知事件保留通用消息处理 |
| `InventoryError` 重复定义 | 已抽到 `inventory.exceptions.InventoryError`，销售、库存、生产服务统一引用 |
| 业务页面入口缺失 | 已新增登录页、退出、工作台、通用列表模板、侧边栏导航 |
| 各 app 路由缺失 | 已接入 `accounts`、`system`、`masterdata`、`bom`、`sales`、`purchase`、`inventory`、`production`、`finance`、`approvals`、`notifications`、`files` 基础路由 |
| 核心列表页面缺失 | 已提供物料、客户、供应商、BOM、销售订单、欠料、采购、库存、生产、财务、审批、消息、附件等只读列表入口 |
| 登录成功默认跳转错误 | 已配置 `LOGIN_REDIRECT_URL=/`，登录后进入工作台 |
| 基础资料只能只读列表 | 已新增物料、客户、供应商创建页和详情页，详情页展示关联产品、地址、供应商价格等信息 |
| 基础资料子表只能查看或导入 | 已新增客户产品、客户地址、物料单位换算和物料供应商价格新增入口，客户地址和供应商价格按个人信息/金额权限控制 |
| 客户产品缺少独立维护页面 | 已新增客户产品详情和编辑页，客户详情与客户产品列表均可跳转，详情页按金额权限展示默认售价 |
| 客户地址录错后只能后台修改 | 已新增客户地址编辑入口，编辑要求个人信息权限，并支持按地址类型切换默认地址 |
| 单位换算和供应商价格录错后只能后台修改 | 已新增物料单位换算编辑和物料供应商价格编辑入口，供应商价格编辑要求金额权限，并支持切换默认供应商价格 |
| 供应商详情核价需要绕回物料页 | 已在供应商详情的供应物料价格表中增加物料详情跳转；有金额权限时可直接进入供应商价格编辑页 |
| 基础资料主表录错后只能通过 Admin 修改 | 已新增物料、客户、供应商编辑入口；无个人信息或金额权限时，编辑页隐藏并保留对应敏感字段 |
| BOM 只能只读列表 | 已新增 BOM 创建页、详情页、草稿/待审/驳回头信息编辑、复制为新版本、明细新增/编辑/删除、启用和停用动作；详情页展示 BOM 头信息和子件明细 |
| 销售订单只能只读列表 | 已新增销售订单创建页、详情页、草稿/驳回编辑、提交审核、审核确认和草稿/待审核/驳回作废动作 |
| 欠料只能只读列表 | 已新增欠料生成采购需求动作页，并接入 `create_purchase_request_from_shortages` |
| 采购需求缺少操作入口 | 已新增采购需求创建页、详情页、草稿/驳回编辑和草稿/待审核/驳回作废动作，详情页展示单头、需求明细和来源欠料 |
| 采购单只能只读列表 | 已新增采购单创建页、详情页、明细新增、草稿/驳回编辑和草稿/待审核/驳回作废动作，并支持从采购需求生成采购单 |
| 进货单只能只读列表 | 已新增进货单详情页、确认入库动作和确认入库前编辑动作，并接入 `confirm_purchase_receipt` |
| 供应商退货只能只读列表 | 已新增供应商退货创建、详情、草稿/驳回编辑、草稿/待审核/驳回作废和确认退货出库动作，并接入 `confirm_supplier_return_shipment` |
| 生产指令缺少人工维护入口 | 已新增生产指令新建、待生产编辑和待生产取消动作，编辑和取消会写审计日志 |
| 生产领料只能只读列表 | 已新增生产领料详情页、确认出库前编辑和确认领料出库动作，并接入 `confirm_material_requisition` |
| 生产入库只能只读列表 | 已新增生产入库详情页、确认入库前编辑和确认生产入库动作，并接入 `confirm_production_receipt` |
| 销售出库只能只读列表 | 已新增销售出库详情页、待确认编辑、待确认作废和确认出库动作，并接入 `confirm_sales_shipment` |
| 客户退货只能只读列表 | 已新增客户退货创建、详情、草稿/驳回编辑、草稿/待审核/驳回作废和确认退货入库动作，并接入 `confirm_customer_return_receipt` |
| 盘点只能只读列表 | 已新增盘点创建页、详情页、明细新增和确认调整库存动作，并接入 `confirm_stock_count_adjustment` |
| 库位移库缺少操作入口 | 已新增库位移库创建页、详情页和确认移库动作，并接入 `confirm_location_transfer` |
| 借样只能只读列表 | 已新增借样创建页、详情页、明细新增、确认借样出库、借样转销售、借样归还列表、归还导出、归还创建、详情、草稿编辑、草稿/待确认作废和确认归还入库动作，并接入 `confirm_sample_loan_out`、`convert_sample_loan_item_to_sales_order`、`confirm_sample_return` |
| 部分已完成列表缺少侧边栏入口 | 已补充借样归还、供应商退货、库位、客户余额、供应商余额和客户产品导航入口，主导航测试覆盖这些页面可访问 |
| 财务只能只读列表 | 已新增客户收款、供应商付款、客户余额、供应商余额详情页，并接入收付款创建、未确认编辑/作废、核销确认、红冲和余额处理动作 |
| 客户收款和供应商付款未确认后无法维护 | 已新增客户收款、供应商付款草稿/待审核编辑和作废动作，编辑会同步未分配金额并写审计日志 |
| 业务单据编辑缺少明确修改原因 | 已为销售订单、销售出库、客户退货、借样归还、采购需求、采购单、进货单、供应商退货、生产指令、生产领料、生产入库、客户收款和供应商付款编辑页补充修改原因，并写入审计日志 |
| 对账单只有列表 | 已新增对账单创建、详情、确认和作废动作；支持客户/供应商按日期范围生成轻量对账单，并动态展示当前未结明细 |
| 对账单无法参与收付款核销 | 已支持客户收款和供应商付款按已确认对账单核销，详情页会展示对账单可核销金额并自动填入建议核销金额 |
| 附件只能只读列表 | 已新增附件上传页、详情页、下载动作和删除动作，下载前写访问日志，删除后不可下载 |
| 收款/付款多单核销存在并发死锁风险 | 已按目标单据 ID 排序后锁定销售订单和进货单，稳定锁顺序 |
| 收付款红冲反向核销明细未锁定 | 已对原核销明细加 `select_for_update()`，避免并发红冲基于同一明细重复计算 |
| 销售/生产库存扣减函数重复 | 已抽取为 `inventory.services.deduct_batch_inventory`，销售出库和生产领料共用同一扣减逻辑 |
| 财务事件标记可能覆盖已确认 payload | `_mark_event_confirmed` 已在事件已确认时直接返回 |
| BOM 草稿无法真正用于销售确认 | 已新增 `bom.services.enable_bom` / `disable_bom`，详情页可启用为默认 BOM 或停用 |
| 新建 BOM 可绕过明细校验直接启用 | 新建页面强制保存为草稿，启用动作统一校验至少一条明细 |
| 销售确认选择启用 BOM 时未优先默认版本 | 已调整为优先锁定同一成品的默认启用 BOM |
| BOM 维护动作缺少独立权限和二次验证 | 已新增 `bom.process` 权限；BOM 新建、编辑、明细新增/编辑/删除、复制、启用和停用均要求该权限，启用/停用还要求当前密码二次验证 |
| BOM 维护动作审计不完整 | BOM 新建、头信息编辑、明细新增/编辑/删除、复制新版本、启用和停用均会写入审计日志；删除、启用和停用要求当前密码与原因 |
| 生产指令新建入口权限口径偏松 | 已要求 `production.process` 权限；列表新建按钮和后端创建页保持一致 |
| 欠料生成采购需求跨模块动作权限不清 | 已要求 `purchase.process` 权限；欠料列表按钮和生成页面后端校验保持一致 |
| 审批只能只读列表 | 已新增审批详情页，展示审批摘要、来源单据、历史审批意见，并接入同意、驳回、转交、退回修改和提交人撤回动作 |
| 审批规则只能通过 Admin 配置 | 已新增审批规则列表、详情和新建页面，并校验审批角色和审批人员至少填写一个 |
| 审批中心普通用户可见全量审批摘要 | 已收紧为普通用户只能查看自己提交或当前待自己审批的审批单；权限管理员和超级管理员可查看全量 |
| 系统消息只能只读列表 | 已新增消息详情页，未读消息打开后自动标记已读，并支持标记已处理、关闭消息和打开来源链接 |
| 角色、权限表未真正关联用户 | 已新增用户-角色、角色-权限关系，补充统一权限判断函数、默认权限数据迁移和模板权限判断标签 |
| 账号和角色只能通过 Django Admin 维护 | 已新增 ERP 用户、角色、权限清单、用户详情、角色详情、用户创建/编辑、角色创建/编辑、登录会话管理和强制失效页面，维护动作要求当前密码、原因并写入审计日志 |
| 列表页新增/导入等操作按钮缺少权限过滤 | 已为通用列表视图新增 `page_action_permissions`，库存、财务和审批规则等列表页已按处理权限、金额权限或权限管理权限过滤按钮 |
| 主数据含金额/个人信息导入入口权限口径不统一 | 已收紧物料、客户产品、供应商价格等含金额导入入口为 `finance.view_amount`，客户、客户地址和供应商等含联系方式导入入口为 `masterdata.view_personal_info`；列表按钮和后端访问保持一致 |
| 审批规则管理无权限保护 | 已要求 `admin.permission_manage` 权限，未授权用户访问返回 403，导航入口按权限隐藏 |
| 敏感附件下载无细粒度权限 | 已要求 `files.attachment_sensitive_view` 权限，未授权用户不能下载敏感附件且不写下载日志 |
| 附件删除无动作权限 | 已要求 `files.attachment_delete` 权限，未授权用户不能删除附件 |
| 销售数据范围未落地 | 已新增 `sales.view_all` 权限；销售订单、欠料、发货、退货、借样和客户资料页面默认只显示自己负责客户或自己创建的数据，有权限用户可看全部 |
| 财务金额字段未脱敏 | 已新增 `finance.view_amount` 权限；客户收款、供应商付款、待处理余额和对账列表/详情金额无权限显示为 `******` |
| 收付款关键动作只靠前端隐藏 | 已新增 `finance.payment_process` 动作权限；客户收款、供应商付款确认/红冲和待处理余额处理均要求同时具备金额查看权限和收付款处理权限 |
| 财务高风险动作缺少二次验证 | 已新增当前密码二次验证；客户收款/供应商付款确认、红冲、作废、待处理余额处理、对账确认/作废均需输入当前登录密码 |
| 财务高风险动作后端权限需防回退 | 已补充客户收款/供应商付款创建、红冲和客户/供应商待处理余额处理的直接 POST 权限回归测试，确保无完整金额权限和收付款处理权限时不能绕过按钮操作 |
| 销售/采购/库存/生产确认类动作只靠登录和状态控制 | 已新增 `sales.process`、`purchase.process`、`inventory.process`、`production.process` 动作权限；销售订单编辑/提交/作废/审核确认/生成出库、销售出库待确认编辑/作废/确认、客户退货编辑/作废/入库、借样追加明细/出库/归还/转销售、借样归还编辑/作废/确认、采购需求编辑/作废、采购单编辑/作废/追加明细/生成进货单、进货单编辑/入库、供应商退货编辑/作废/出库、盘点创建/新增明细/调整、库位移库创建/确认、生产指令编辑/取消/生成领料/生成入库、生产领料编辑/确认和生产入库编辑/确认均接入后端权限校验 |
| 新建/编辑页“保存并提交”可能绕过处理权限 | 已要求销售订单、客户退货、采购需求、采购单和供应商退货在 `action=submit` 时具备对应处理权限；普通用户仍可保存草稿，模板按权限隐藏提交按钮 |
| 非财务高风险动作缺少二次验证 | 已抽取通用 `require_second_verify`，并接入销售订单审核/作废、销售出库确认/作废、客户退货入库/作废、借样出库、借样归还确认/作废、采购需求作废、采购单作废、进货入库、供应商退货出库/作废、期初库存确认/撤销、库位移库确认、盘点调整确认、生产指令取消、生产领料确认和生产入库确认 |
| 作废、取消和库存调整类高风险动作缺少原因闭环 | 已新增通用 `require_post_reason`，销售订单、销售出库、客户退货、借样归还、采购需求、采购单、供应商退货、客户收款、供应商付款、对账单作废，生产指令取消，盘点调整确认和期初库存撤销均要求填写原因，并写入审计日志快照 |
| 销售、采购和主数据价格仍可见 | 已复用 `finance.view_amount` 权限；销售订单、客户退货、采购单、进货单、供应商退货、物料最新采购价、供应商报价、客户产品默认售价无权限显示为 `******` |
| 无金额权限用户录单可能被价格字段卡住 | 销售订单无权限时按客户产品默认售价带入，采购单无权限时按供应商/物料默认采购价带入；借样转销售因涉及价格决策，暂要求金额权限 |
| 客户和供应商联系信息裸露 | 已新增 `masterdata.view_personal_info` 权限；客户电话、客户地址收件人和电话、供应商联系人和电话无权限显示为 `******` |
| 基础资料创建和导入可绕过个人信息权限 | 客户和供应商创建页无个人信息权限时会忽略 POST 中偷传的联系方式；客户、客户地址和供应商 CSV 导入及模板下载均要求 `masterdata.view_personal_info` |
| 附件只校验登录和敏感权限 | 已新增附件来源单据权限校验；上传、列表、详情、下载、删除均要求用户能访问来源单据或为上传人，敏感附件仍叠加 `files.attachment_sensitive_view` |
| 对账单附件来源权限缺失 | 已补充 `reconciliation` 来源单据权限映射，只有具备财务金额查看权限的用户可上传、查看和下载对账单附件 |
| 附件重复删除可重复写日志 | `delete_attachment` 已加行级锁和状态校验，已删除附件二次删除会返回状态错误且不再写重复删除日志 |
| 历史导出 CSV 可能被权限管理员下载他人未脱敏文件 | 导出日志仍允许权限管理员审计查看，但历史 CSV 文件下载已收紧为仅导出本人或超级管理员可下载 |
| 历史导出 CSV 路径可能被数据库篡改 | 导出日志下载已限制为 `MEDIA_ROOT/exports/<导出号>.csv`，详情页和下载接口共用同一校验，外部路径或文件名不匹配返回文件不存在 |
| 各模块即时导出响应逻辑重复 | 已新增统一 `export_file_response`，BOM、主数据、销售、采购、库存、生产和财务导出统一复核文件路径后下载 |
| CSV 导出存在 Excel 公式注入风险 | 通用导出服务已对公式样式文本加纯文本前缀，避免用户用 Excel 打开导出文件时执行公式 |
| 采购、生产、库存、财务运营单据附件权限偏松 | 已将采购需求、采购单、进货单、供应商退货附件收紧到 `purchase.process`；生产指令、生产领料、生产入库附件收紧到 `production.process`；库位移库、盘点附件收紧到 `inventory.process`；客户收款、供应商付款、客户/供应商待处理余额附件收紧到 `finance.view_amount`；销售附件继续遵守销售数据范围 |
| 部分已有来源权限的业务单据无内嵌附件入口 | 已补齐采购需求、生产指令、客户待处理余额、供应商待处理余额和审批详情页附件面板，避免用户只能从附件中心上传、回到业务单据却看不到附件 |
| 附件上传可提交伪造来源单号 | 上传时后端按 `source_doc_type + source_doc_id` 解析真实单号，入库不再使用前端隐藏字段里的单号 |
| 附件物理文件缺失仍可能写下载日志 | 附件下载现在先确认物理文件可打开，再写访问日志；文件缺失返回 404 且不产生虚假的下载记录 |
| 单据详情页不能直接上传附件 | 已新增可复用附件面板，并接入销售、采购、生产、库存和财务主要业务单据详情页；面板使用隐藏来源字段上传，不再要求用户手工填写来源单据 |
| 工作台销售摘要绕过销售数据范围 | 已将待审销售订单、最近销售订单和欠料提醒按 `sales.view_all` / 负责人 / 创建人范围过滤，避免普通销售在首页看到他人客户数据 |
| 附件访问日志缺少汇总查询入口 | 已新增附件访问日志列表和筛选页，支持按文件/来源单号/附件号、动作、操作人和日期过滤，并仅允许权限管理员访问 |
| 财务核销页面仍需人工计算余额 | 已在客户收款和供应商付款详情页展示订单、进货单和已确认对账单的可核销金额，并按本次收付款金额自动填入建议核销金额 |
| 已确认对账单明细动态重算导致历史漂移 | 已新增 `reconciliation_items` 快照表；对账确认时固化来源单据、日期、原始金额、调整金额、已核销金额和未结金额，详情与打印优先读取快照 |
| 后台任务缺少执行与查询入口 | 已新增 `backup_daily`、`verify_backups`、`process_pending_events`、`cleanup_backups`、`restore_drill` 等管理命令，并通过 `background_jobs` 防止同类任务重复运行；后台任务列表可按关键词和状态查询 |
| 备份只有模型没有实现 | 已新增每日备份服务，生成数据库 JSON 转储和附件目录压缩包，记录备份文件大小和 SHA-256 校验值 |
| 备份校验未实现 | 已新增备份校验服务，可检查备份文件存在性、SHA-256 校验值和 zip 可读性 |
| 缺少管理员健康检查入口 | 已新增 `/health/` 系统健康检查页，展示数据库连接、附件目录、备份目录、最近备份、最近后台任务和失败任务，并可跳转备份记录和后台任务列表 |
| 备份保留策略和旧备份清理未落地 | 已新增 `cleanup_backups` 服务和管理命令，按最近每日、周备份、月备份和失败备份保留天数清理旧文件与记录 |
| 恢复演练命令缺失 | 已新增 `restore_drill` 服务和管理命令，只读校验备份包结构、SHA-256、`database.json` 格式和附件目录可读性，不覆盖当前数据库 |
| 备份恢复演练存在 zip 路径穿越风险 | 恢复演练已改为安全解压，逐个成员校验目标路径必须留在演练目录内，不安全备份包会被拒绝 |
| 旧备份清理可能误删备份目录外文件 | 清理服务已限制删除目标必须位于 `ERP_BACKUP_DIR` 内；异常路径会返回部分失败并保留备份记录 |
| 停用、锁定或逻辑删除账号仍可能登录 | 已新增 ERP 登录表单校验，非启用状态账号会被拒绝登录，避免用户进入系统后只靠权限为空来兜底 |
| `UserSession` 只有模型没有实际作用 | 已新增会话追踪中间件，访问时维护会话记录并执行 revoked 会话强制退出；`session_key` 已加唯一约束并生成 `accounts.0010` |
| 管理员可能误锁定自己的账号或权限 | 用户/角色维护表单已阻止停用自己的当前账号、逻辑删除自己、移除自己的最后一个权限管理角色或停用自己最后一个权限管理角色 |
| 生产部署和任务调度缺少可执行手册 | 已新增生产上线部署与运维手册，明确 `.env`、PostgreSQL、静态文件、Nginx、Windows 任务计划程序、Linux cron、上线检查和恢复原则 |
| 导出能力只有模型没有入口 | 已新增通用 CSV 导出服务，物料、客户、客户产品、供应商、库存汇总、库存批次、库存流水列表已接入“导出CSV”，导出后写入 `export_logs` |
| 业务单据列表缺少导出 | 已新增销售订单、欠料提醒、销售出库、客户退货、借样、借样归还、采购需求、采购单、进货单、供应商退货、库位移库、盘点、生产指令、生产领料和生产入库列表 CSV 导出，销售导出遵守数据范围，金额字段按权限脱敏 |
| 财务列表缺少导出 | 已新增客户收款、供应商付款、客户待处理余额、供应商待处理余额和对账单 CSV 导出，金额按权限脱敏 |
| 列表缺少筛选 | 已新增通用列表搜索、状态筛选、分页参数保留和筛选后导出；物料、客户、客户产品、供应商、BOM、销售订单、欠料提醒、销售出库、客户退货、借样、采购需求、采购单、进货单、供应商退货、库存批次、库存流水、库位移库、盘点、生产指令、生产领料、生产入库、客户收款、供应商付款、待处理余额、对账单、审批、审批规则和附件已接入 |
| 保存筛选条件未落地 | 已新增通用保存筛选入口，所有继承 `ErpListView` 的列表页均可保存当前筛选、设为默认、套用和删除，筛选按用户隔离 |
| 保存筛选后的 `return_to` 可被构造为外部跳转 | 已限制保存、设默认和删除筛选后的跳转目标必须为本站 URL，外部 URL 会回到工作台 |
| 库位只能列表和导入 | 已新增库位详情、新建和编辑页面，新建/编辑要求 `inventory.process` 权限 |
| 库存汇总和库存流水缺少详情追溯 | 已新增库存汇总详情页和库存流水详情页，库存汇总可查看相关批次与近期流水，库存流水可反查批次和库存汇总 |
| 库存高频查询筛选不足 | 已新增库存汇总按物料/库位关键词搜索、库位列表按关键词和状态筛选、库存流水按流水号/来源单据/物料/库位/批次关键词搜索和流水类型筛选；库存汇总、库位列表和库存流水筛选后导出沿用当前查询条件 |
| 消息中心筛选和批量处理缺失 | 已新增系统消息关键词搜索、状态筛选、级别筛选，以及批量标记已处理和批量关闭 |
| 消息中心稍后提醒缺失 | 已新增单条和批量稍后提醒，支持 1 小时后提醒、明天 9 点提醒；未到期消息默认从列表和工作台隐藏，到期后打开列表或工作台自动恢复为未读 |
| 打印日志只有模型没有入口 | 已新增销售订单、销售出库、客户退货、借样、借样归还、采购单、进货入库、供应商退货、库位移库、盘点、生产指令、生产领料、生产入库、客户收款、供应商付款、客户待处理余额、供应商待处理余额和对账单打印页，打开打印页会写入 `PrintLog`，并按金额权限脱敏价格、金额和成本 |
| 导入日志只有模型没有入口 | 已新增物料、物料单位换算、物料供应商价格、客户、客户地址、客户产品、供应商、库位 CSV 导入模板下载和导入页面；期初库存 CSV 导入会写 `InitializationJob`，并同时生成库存批次、库存汇总和库存流水 |
| CSV 导入缺少文件大小和行数限制 | 已新增统一 CSV 上传大小校验和 `ERP_MAX_CSV_IMPORT_ROWS` 行数限制；所有 CSV 导入共用安全读取入口，畸形 CSV 会落导入任务失败明细 |
| 期初库存导入缺少预览确认和撤销能力 | 已改为上传后先生成待确认初始化任务并展示预览明细，确认后入账；未使用的成功期初导入可撤销，撤销会作废批次、扣回库存汇总并写反向库存流水 |
| 销售订单上线初期只能手工录入 | 已新增销售订单 CSV 导入，支持一行一明细、多行同订单号合并为一张草稿订单；有金额权限时使用 CSV 单价，无金额权限时自动使用客户产品默认售价 |
| 销售出库上线初期只能手工录入 | 已新增销售出库 CSV 导入，支持一行一明细、多行同出库单号合并为一张待确认出库单；导入会校验销售数据范围、未发货数量、批次可用库存和同批次占用，不直接扣库存 |
| 采购需求上线初期只能手工录入 | 已新增采购需求 CSV 导入，支持一行一明细、多行同需求单号合并为一张草稿需求；导入后仍需按正常采购流程生成采购单 |
| 采购单上线初期只能手工录入 | 已新增采购单 CSV 导入，支持一行一明细、多行同采购单号合并为一张草稿采购单；有金额权限时使用 CSV 单价，无金额权限时自动使用供应商默认采购价或物料最新采购价 |
| 进货单上线初期只能从采购单页面生成 | 已新增进货单 CSV 导入，支持一行一明细、多行同进货单号合并为一张待入库进货单；导入会校验来源采购单、采购行剩余未到货数量、到货/合格/不合格数量和入库库位，不直接生成库存批次 |
| 借样单上线初期只能手工录入 | 已新增借样单 CSV 导入，支持一行一明细、多行同借样单号合并为一张待审核借样单；可预填批次和库位，但导入不扣库存 |
| 生产领料上线初期只能从生产指令页面生成 | 已新增生产领料 CSV 导入，支持一行一明细、多行同领料单号合并为一张待确认领料单；导入会校验生产指令、锁定 BOM 子件、批次可用库存和库位，不直接扣库存 |
| 生产入库上线初期只能从生产指令页面生成 | 已新增生产入库 CSV 导入，支持一行一明细、多行同入库单号合并为一张待确认生产入库单；导入会校验生产指令状态、剩余未入库数量、库位、批次号和质量状态，不直接生成批次或库存流水 |
| 客户收款和供应商付款上线初期只能手工录入 | 已新增客户收款和供应商付款 CSV 导入，导入会校验客户/供应商状态、日期、金额和收付款方式，只生成待审核单据，后续仍需确认并核销 |
| 运维日志写入后缺少后台查询入口 | 已新增导入任务、初始化任务、导出日志、打印日志、备份记录和后台任务列表页，均要求权限管理员访问，并支持关键词、状态等基础筛选；导入任务和初始化任务详情页可查看校验错误明细；导出日志详情页支持查看筛选条件并下载历史 CSV 文件；打印日志详情页可查看来源单据和打印人 |
| 审计日志只有模型和 Admin | 已新增通用审计日志服务、管理员审计日志列表页，并接入销售、采购、库存、生产、财务关键确认、红冲和余额处理动作 |
| 权限拒绝和页面不存在使用默认错误页 | 已新增自定义 403/404 错误页，权限拒绝会展示具体原因，404 会提供返回工作台或登录入口 |

本轮新增页面测试：

- 未登录访问工作台和列表页会跳转登录页。
- 登录页可渲染，登录成功跳转工作台。
- 工作台可渲染。
- 侧边栏主要导航目标均可访问。
- 通用 pending event 处理后会标记成功并生成消息。
- 销售订单可以通过页面创建草稿。
- 销售订单可以通过页面保存并提交审核。
- 销售订单详情页可以触发审核确认，并接入 BOM 锁定、库存检查和欠料生成。
- 销售订单草稿或已驳回状态可以编辑，编辑会重算金额、写变更日志和审计日志；修改原因会进入变更日志和审计日志。
- 销售订单草稿、待审核或已驳回状态可以作废，作废会写变更日志和审计日志。
- 销售订单作废必须填写原因，原因会写入变更日志和审计日志。
- 欠料生成采购需求页面可以选择未处理欠料，并跳转采购需求详情。
- 采购需求详情页可以展示来源欠料生成的明细。
- 采购需求可以通过页面人工创建，支持保存草稿或提交审核。
- 人工采购需求草稿或已驳回状态可以编辑，编辑会重排行号并写审计日志。
- 采购需求草稿、待审核或已驳回状态可以作废，作废会关闭明细并写审计日志。
- 已生成下游采购单的采购需求不能编辑。
- 采购需求详情页可以选择供应商生成采购单，并同步更新采购需求明细状态。
- 采购单页面可以手工新建单头和多行明细，详情页可继续新增明细。
- 采购单草稿或已驳回状态可以编辑，编辑会重算金额、重排行号并写审计日志，修改原因会进入审计日志。
- 采购单草稿、待审核或已驳回状态可以作废，作废会更新状态并写审计日志。
- 采购单作废必须填写原因，原因会写入审计日志。
- 已通过采购单不能进入编辑页面。
- 采购单详情页可以按未到货数量生成进货单，并统一指定本次入库库位。
- 进货单待入库状态可以编辑到货数量、合格数量、不合格数量和库位，编辑会写审计日志。
- 已入库或已生成批次的进货单不能编辑。
- 进货单详情页可以触发确认入库，生成批次、更新库存和库存流水。
- 供应商退货页面可以新建退货单、草稿/驳回编辑、草稿/待审核/驳回作废，并在已确认后触发退货出库，扣减批次库存并写 `supplier_return_out` 库存流水。
- 采购入库后可以触发欠料重检，销售订单明细变为已齐套。
- 生产领料待确认状态可以编辑实领数量、批次、库位和调整原因，编辑会写审计日志。
- 生产领料编辑会校验批次物料、批次库位、需求数量和批次剩余库存。
- 生产领料详情页可以触发确认领料出库，扣减批次库存和库存汇总，并更新生产单状态。
- 生产入库待确认状态可以编辑入库数量、库位、批次号和质检状态，编辑会写审计日志。
- 生产入库编辑会校验入库数量不能超过生产指令剩余未入库数量。
- 生产入库详情页可以触发确认生产入库，增加成品库存，并更新生产单和销售订单明细状态。
- 生产指令页面可以人工新建生产指令，并自动锁定选择的启用 BOM 版本。
- 生产指令待生产且无领料/入库单时可以编辑生产数量、计划日期、BOM 和备注，并写审计日志，修改原因会进入审计日志。
- 生产指令待生产且无领料/入库单时可以取消，并写审计日志。
- 生产指令取消必须填写原因，原因会写入审计日志。
- 生产指令详情页可以查看 BOM 子件、领料单和入库单，并可生成生产领料单和生产入库单。
- 销售出库单在待确认状态下可以编辑出库数量、批次、库位或作废，确认后扣减成品批次库存并更新销售订单发货状态。
- 客户退货页面可以新建退货单、草稿/驳回编辑、草稿/待审核/驳回作废，并在已确认后触发退货入库，生成退货批次、更新库存汇总并写 `customer_return_in` 库存流水。
- 盘点创建页可以按当前在库批次生成账面数量快照。
- 盘点详情页可以触发确认调整库存，完成盘盈盘亏、库存汇总更新和库存流水写入。
- 盘点调整确认必须填写原因，原因会写入审计日志。
- 盘点详情页可以手工新增盘盈或差异明细。
- 库位移库页面可以创建移库单，确认后扣减原库位批次库存，并在目标库位生成移入批次和移库流水。
- 借样页面可以新建借样单和借样明细，详情页可继续新增待审核明细。
- 借样详情页可以确认借样出库，扣减批次库存、更新库存汇总并写 `sample_out` 库存流水。
- 借样详情页可以将未归还未转销售数量转为待审核销售订单，并写 `sample_to_sales` 库存流水，不重复扣减库存。
- 借样详情页可以登记归还，借样归还单支持草稿编辑、草稿/待确认作废和确认归还入库，并校验归还数量不能超过未归还且未转销售数量。
- 物料、客户、供应商页面可以新建基础资料，并在详情页查看关联单位换算、客户产品、地址和供应商价格。
- 物料、客户、供应商详情页可以进入编辑页，编辑会更新版本号和更新人；无权限用户不会看到或覆盖敏感金额/联系方式字段。
- 客户详情页可以新增客户产品和客户地址；客户地址新增要求个人信息权限。
- 物料详情页可以新增单位换算和供应商价格；供应商价格新增要求金额权限。
- BOM 页面可以新建 BOM 头，在详情页维护子件明细，支持用量、单位、损耗率和必需/可选标记。
- BOM 详情页可以启用为默认 BOM 或停用，启用前会校验至少一条明细。
- BOM 新建、头信息编辑、明细新增/编辑/删除、复制新版本、启用和停用均写入审计日志；高风险启停和删除动作要求当前密码与原因。
- 销售订单确认会优先锁定默认启用 BOM。
- 销售订单详情页可以按库存充足且未发货明细自动生成销售出库单，并按 FIFO 推荐出库批次。
- 客户收款和供应商付款页面可以新建单据，在详情页选择目标订单、进货单或已确认对账单填写核销金额并确认。
- 客户收款和供应商付款草稿/待审核状态可以编辑金额、日期、方式、往来单位和备注，编辑会同步未分配金额并写审计日志，修改原因会进入审计日志。
- 客户收款和供应商付款草稿/待审核状态可以作废，作废会写审计日志。
- 客户收款和供应商付款作废必须填写原因，原因会写入审计日志。
- 客户收款和供应商付款编辑/作废动作要求同时具备金额查看权限和收付款处理权限。
- 客户收款和供应商付款详情页可以查看核销明细、红冲记录，并触发红冲动作。
- 对账单页面可以按客户或供应商、日期范围生成草稿对账单，详情页展示来源销售订单或进货单的原始金额、退货/调整、已核销和未结金额。
- 对账单草稿可确认，草稿或已确认但未核销对账单可作废，确认和作废会写审计日志。
- 对账单作废必须填写原因，原因会写入审计日志。
- 对账单详情要求 `finance.view_amount` 权限，确认和作废要求同时具备 `finance.payment_process` 权限。
- 已确认客户对账单可作为客户收款核销目标，已确认供应商对账单可作为供应商付款核销目标。
- 客户待处理余额和供应商待处理余额详情页可以查看处理记录，并触发关闭、退款、转预收或转预付等处理动作。
- 附件页面可以上传实际文件、查看详情、下载文件、逻辑删除附件，并记录下载和删除日志。
- 客户收款核销会按销售订单 ID 排序处理，降低并发死锁风险。
- 供应商付款核销会按进货单 ID 排序处理，降低并发死锁风险。
- 收付款核销同时支持对账单目标，服务层先锁定对账单再锁定订单或进货单，降低与对账单确认流程的反向锁风险。
- 审批列表可以进入详情，当前审批人可在详情页同意、驳回、转交或退回修改审批。
- 审批详情页会显示 `source_summary` 摘要快照和历史审批记录，非当前审批人不能看到处理按钮，提交人可撤回待审批单。
- 审批动作日志会记录 IP 和浏览器信息，满足远程审批审计要求。
- 审批规则可以通过页面新建、查看列表和详情，并展示条件 JSON、审批人、审批角色、二次验证等配置。
- 系统消息列表可以进入详情，未读消息打开后自动标记已读。
- 系统消息详情页可以标记已处理或关闭消息，其他用户不能访问不属于自己的消息。
- 用户可以通过 ERP 角色获得权限，超级管理员默认拥有全部 ERP 权限，停用用户不能继续获得权限。
- 权限管理员可以通过 ERP 页面查看、新建和编辑用户，分配角色，并查看用户有效权限和最近登录会话。
- 权限管理员可以通过 ERP 页面查看、新建和编辑角色，分配权限，并查看角色下的用户。
- 权限管理员可以通过 ERP 页面查看权限清单、登录会话，并强制失效非当前登录会话。
- 用户可以在 ERP 顶部栏自助修改登录密码。
- 用户和角色维护会要求当前密码与操作原因，且会写审计日志；系统会阻止管理员把自己的最后一个权限管理入口移除。
- 审批规则管理入口会按权限隐藏，服务端仍会对无权限访问返回 403。
- 敏感附件下载和附件删除已接入权限校验，普通附件仍可按登录状态下载。
- 默认销售用户只能查看自己负责客户或自己创建的销售数据。
- 拥有 `sales.view_all` 权限的用户可以查看全部销售订单。
- 客户和客户产品页面已按销售数据范围过滤。
- 无 `finance.view_amount` 权限时，财务列表和详情页金额字段会脱敏显示。
- 收付款确认、红冲和待处理余额处理按钮已要求 `finance.payment_process`，后端手工 POST 无权限会返回 403。
- 销售、采购、库存和生产确认或关键编辑类按钮已要求对应模块处理权限，后端手工访问无权限会返回 403。
- 销售订单、客户退货、采购单、进货单、供应商退货、物料和供应商价格详情已接入金额脱敏。
- 无金额权限录入销售订单和采购明细时，系统会使用默认价格计算单据金额。
- 借样转销售入口已要求金额权限，避免无权限用户直接录入销售单价。
- 客户和供应商联系信息已接入 `masterdata.view_personal_info` 权限脱敏。
- 默认权限数据迁移已补齐 `masterdata.view_personal_info`。
- 附件列表、详情、下载、删除已按来源单据权限过滤。
- 附件上传时会校验来源单据存在且当前用户有权访问。
- 销售订单附件已按销售数据范围控制，拥有 `sales.view_all` 的用户可查看全部销售订单附件。
- 销售订单、销售出库、客户退货、借样、借样归还、采购单、进货单、供应商退货、生产领料、生产入库、库位移库、盘点、客户收款、供应商付款详情页已显示附件列表并支持内嵌上传。
- 敏感附件在单据附件面板中会按 `files.attachment_sensitive_view` 控制下载入口。
- 附件访问日志列表可按文件、来源单号、动作、操作人和日期筛选，普通用户访问返回 403。
- 客户收款核销页面会展示销售订单可核销金额，并按收款金额自动填入建议核销金额。
- 供应商付款核销页面会展示进货单可核销金额，并按付款金额自动填入建议核销金额。
- 每日备份命令可以生成数据库与附件备份 zip，并写入 `backups` 与 `background_jobs`。
- 备份校验命令可以发现备份文件缺失、校验值不一致或压缩包损坏。
- `process_pending_events` 已提供管理命令入口，并记录后台任务执行结果。
- 系统健康检查页仅允许权限管理员访问，普通用户访问返回 403。
- 系统健康检查页会展示数据库连接、附件目录可写、备份目录可写、最近备份和最近后台任务。
- 旧备份清理命令会保留最近 30 天成功备份、更早的周/月代表备份，并清理过期失败备份。
- 物料、客户、客户产品、供应商列表支持导出 CSV，导出文件使用 UTF-8 BOM，方便中文 Excel 直接打开。
- 每次列表导出会写入 `ExportLog`，记录模块、文件路径、行数和导出人。
- 客户和客户产品导出遵守销售数据范围；供应商导出联系人按个人信息权限脱敏。
- 库存汇总、库存批次和库存流水支持导出 CSV，并写入导出日志。
- 销售订单、欠料提醒、销售出库、客户退货、借样、采购需求、采购单、进货单、供应商退货、库位移库、盘点、生产指令、生产领料和生产入库列表支持导出 CSV，并写入导出日志。
- 销售订单导出遵守销售数据范围，并按 `finance.view_amount` 权限脱敏金额。
- 采购单导出按 `finance.view_amount` 权限脱敏金额。
- 客户收款、供应商付款、客户待处理余额、供应商待处理余额和对账单列表支持导出 CSV，并按 `finance.view_amount` 权限脱敏金额。
- 通用列表模板支持关键词搜索、状态筛选、清除筛选和分页参数保留。
- 销售订单、欠料提醒、销售出库、客户退货、借样、采购需求、采购单、进货单、供应商退货、库存批次、库位移库、盘点、生产指令、生产领料、生产入库、客户收款、供应商付款、客户待处理余额、供应商待处理余额和对账单列表已接入关键词与状态筛选。
- 带筛选条件的导出会沿用当前筛选参数，并在 `ExportLog.filter_json` 中记录查询条件。
- 系统消息列表支持关键词、状态和级别筛选。
- 系统消息列表支持勾选多条消息后批量标记已处理或批量关闭，且只能处理当前用户自己的消息。
- 销售订单打印页会写入 `PrintLog`。
- 无 `finance.view_amount` 权限访问销售订单打印页时，单价与金额会脱敏。
- 无销售数据访问权限的用户访问他人销售订单打印页会返回 404，且不会写打印日志。
- 销售出库打印页会写入 `PrintLog`，成本价按 `finance.view_amount` 权限脱敏。
- 客户退货打印页会写入 `PrintLog`，单价和金额按 `finance.view_amount` 权限脱敏，并遵守销售数据范围。
- 采购单打印页会写入 `PrintLog`，采购单价和金额按 `finance.view_amount` 权限脱敏。
- 进货入库打印页会写入 `PrintLog`，采购单价按 `finance.view_amount` 权限脱敏。
- 供应商退货打印页会写入 `PrintLog`，单价和金额按 `finance.view_amount` 权限脱敏。
- 生产指令打印页会写入 `PrintLog`，展示成品、计划信息和锁定 BOM 子件。
- 生产领料打印页会写入 `PrintLog`。
- 生产入库打印页会写入 `PrintLog`。
- 借样和借样归还打印页会写入 `PrintLog`，并遵守销售数据范围。
- 库位移库和盘点打印页会写入 `PrintLog`。
- 客户收款和供应商付款打印页会写入 `PrintLog`，金额按 `finance.view_amount` 权限脱敏。
- 客户待处理余额和供应商待处理余额打印页会写入 `PrintLog`，金额按 `finance.view_amount` 权限脱敏。
- 对账单打印页会写入 `PrintLog`，并要求 `finance.view_amount` 金额查看权限。

## 2. 当前已完成内容

### 2.1 项目基础

已完成 Django 单体项目基础结构：

- 项目配置包：`config`
- 已注册 app：
  - `accounts`
  - `masterdata`
  - `bom`
  - `sales`
  - `purchase`
  - `inventory`
  - `production`
  - `finance`
  - `approvals`
  - `notifications`
  - `files`
  - `system`
- 已配置自定义用户模型：`accounts.User`
- 已支持本地 SQLite 默认开发数据库，环境变量可切换 PostgreSQL
- 已补充生产安全基础配置：
  - `DJANGO_ENV=production` 时必须提供 `DJANGO_SECRET_KEY`
  - `DJANGO_DEBUG` 默认关闭
  - `LOGIN_URL=/login/`，`LOGIN_REDIRECT_URL=/`
  - Session 默认 8 小时过期
  - 已配置基础日志输出到 `logs/erp.log`
- 已配置中文语言与香港时区：`zh-hans`、`Asia/Hong_Kong`
- 已创建本地开发管理员账号：
  - 用户名：`admin`
  - 密码：`Admin@2026!`

### 2.2 数据模型与迁移

核心业务表已建模并完成迁移，覆盖以下模块：

- 账号、角色、权限、用户会话
- 物料、单位换算、客户、客户产品、地址、供应商、供应商价格
- BOM 与 BOM 明细
- 销售订单、销售订单明细、客户退货、借样、借样归还、销售出库、欠料提醒
- 采购需求、采购单、进货单、供应商退货
- 库位、库存批次、库存汇总、库存流水、移库、盘点
- 生产指令、生产领料、生产入库
- 客户收款、供应商付款、核销、红冲、待处理余额、对账单
- 审批单、审批规则、审批日志
- 系统消息
- 附件、附件访问日志、导入任务、初始化任务、导出日志、打印日志
- 单号序列、事务后事件、后台任务、系统配置、审计日志、备份记录、发布记录

核心模型已注册到 Django Admin，当前可以通过后台查看和维护基础数据。

本轮模型复核后，以下关键一致性问题已修复：

- BOM 已补齐基准数量、生效日期、失效日期、默认 BOM 标记。
- 客户地址已支持地址类型区分。
- 销售订单已补齐审核人和审核时间字段。
- 欠料提醒与采购需求已建立真实外键关系。

### 2.3 已完成服务层

已实现的服务入口如下：

| 模块 | 服务方法 | 当前能力 |
| --- | --- | --- |
| `sales` | `confirm_sales_order` | 销售订单审核确认、BOM 锁定、库存检查、欠料生成 |
| `sales` | `recheck_sales_order_inventory` | 采购入库或库存变化后重新检查齐套状态 |
| `sales` | `confirm_sales_shipment` | 销售出库、扣减成品库存、更新发货状态 |
| `sales` | `confirm_customer_return_receipt` | 客户退货入库、生成退货批次、写库存流水 |
| `sales` | `confirm_sample_loan_out` | 借样出库、扣减库存、写库存流水 |
| `sales` | `convert_sample_loan_item_to_sales_order` | 借样转销售、生成待审核销售订单、更新借样转销售数量 |
| `sales` | `confirm_sample_return` | 借样归还入库、更新借样状态、关闭逾期 |
| `purchase` | `create_purchase_request_from_shortages` | 欠料提醒生成采购需求 |
| `purchase` | `confirm_purchase_receipt` | 采购入库、生成批次、更新库存和采购状态 |
| `purchase` | `confirm_supplier_return_shipment` | 供应商退货出库、扣减库存、写库存流水 |
| `production` | `confirm_material_requisition` | 生产领料、扣减子件库存、更新生产状态 |
| `production` | `confirm_production_receipt` | 生产入库、增加成品库存、更新销售可发货状态 |
| `inventory` | `confirm_location_transfer` | 库位移库、扣减原库位库存、增加目标库位库存并写流水 |
| `inventory` | `confirm_stock_count_adjustment` | 盘点调整、盘盈盘亏、写库存流水 |
| `finance` | `confirm_customer_receipt` | 客户收款确认、按销售订单核销、生成未分配余额 |
| `finance` | `reverse_customer_receipt` | 客户收款红冲、生成反向核销 |
| `finance` | `apply_customer_credit_balance` | 客户待处理余额关闭、转预收、转核销等基础处理 |
| `finance` | `confirm_supplier_payment` | 供应商付款确认、按进货单核销、生成未分配余额 |
| `finance` | `reverse_supplier_payment` | 供应商付款红冲、生成反向核销 |
| `finance` | `apply_supplier_credit_balance` | 供应商待处理余额基础处理 |
| `approvals` | `apply_approval_action` | 审批通过、驳回、转交、退回修改、提交人撤回，并写审批日志和远程审计信息 |
| `notifications` | `create_system_message` | 创建系统消息 |
| `notifications` | `mark_message_read` | 消息标记已读 |
| `notifications` | `mark_message_processed` | 消息标记已处理 |
| `files` | `register_attachment` | 附件元数据登记、文件类型与大小校验 |
| `files` | `delete_attachment` | 附件逻辑删除 |
| `files` | `record_attachment_access` | 附件访问日志 |
| `system` | `next_document_no` | 单号生成 |
| `system` | `enqueue_pending_event` | 事务后事件入队 |
| `system` | `record_audit_log` / `record_audit_log_from_request` | 通用审计日志记录，支持操作人、来源单据、IP、User-Agent 和快照 |
| `system` | `process_pending_events` | 事务后事件按类型分发，支持采购入库后欠料重检和消息通知 |
| `system` | `start_background_job` / `finish_background_job` / `fail_background_job` | 后台任务运行记录和同类任务防重复 |
| `system` | `backup_daily` | 生成每日数据库和附件备份 zip，记录文件大小和 SHA-256 |
| `system` | `verify_backups` | 校验备份文件存在性、校验值和压缩包可读性 |
| `system` | `cleanup_backups` | 按保留策略清理旧备份文件和记录 |
| `system` | `restore_drill` | 执行只读式备份恢复演练，校验数据库转储和附件包可读性 |
| `files` | `export_queryset_to_csv` | 通用 CSV 导出，生成文件并写导出日志 |
| `files` | `record_print_log` | 通用打印日志记录，生成打印流水号并写 `PrintLog` |
| `masterdata` | `import_materials_from_csv` | 物料 CSV 导入，校验后批量创建物料并写 `ImportJob` |
| `masterdata` | `import_customers_from_csv` | 客户 CSV 导入，要求个人信息权限，校验客户编号和销售负责人账号并写 `ImportJob` |
| `masterdata` | `import_customer_products_from_csv` | 客户产品 CSV 导入，校验客户编号、客户产品编号和关联成品编码并写 `ImportJob` |
| `masterdata` | `import_material_unit_conversions_from_csv` | 物料单位换算 CSV 导入，校验物料编码、单位、换算比例和重复换算并写 `ImportJob` |
| `masterdata` | `import_customer_addresses_from_csv` | 客户地址 CSV 导入，要求个人信息权限，校验客户编号、地址类型、默认地址和状态并写 `ImportJob` |
| `masterdata` | `import_material_supplier_prices_from_csv` | 物料供应商价格 CSV 导入，要求金额权限，校验物料、供应商、采购价、日期和默认价格并写 `ImportJob` |
| `masterdata` | `import_suppliers_from_csv` | 供应商 CSV 导入，要求个人信息权限，校验供应商编号和状态并写 `ImportJob` |
| `inventory` | `import_warehouse_locations_from_csv` | 库位 CSV 导入，校验库位编码、名称和状态并写 `ImportJob` |
| `inventory` | `import_initial_inventory_from_csv` | 期初库存 CSV 导入，校验物料、库位、批次、库存类型、数量和成本后写 `InitializationJob`、库存批次、库存汇总和期初库存流水 |

### 2.4 已完成页面骨架

当前已从“只有 Admin”推进到“有业务页面入口”的阶段：

- 登录页：`/login/`
- 退出：`/logout/`
- 工作台首页：`/`
- 基础资料：
- `/masterdata/materials/`
- `/masterdata/materials/export/`
- `/masterdata/materials/import-template/`
- `/masterdata/materials/import/`
- `/masterdata/materials/new/`
  - `/masterdata/materials/<id>/`
- `/masterdata/customers/`
- `/masterdata/customers/export/`
- `/masterdata/customers/import-template/`
- `/masterdata/customers/import/`
- `/masterdata/customers/new/`
- `/masterdata/customers/<id>/`
- `/masterdata/customer-products/`
- `/masterdata/customer-products/export/`
- `/masterdata/customer-products/import-template/`
- `/masterdata/customer-products/import/`
- `/masterdata/suppliers/`
- `/masterdata/suppliers/export/`
- `/masterdata/suppliers/import-template/`
- `/masterdata/suppliers/import/`
- `/masterdata/suppliers/new/`
  - `/masterdata/suppliers/<id>/`
- BOM：
  - `/bom/`
  - `/bom/new/`
  - `/bom/<id>/`
  - `/bom/<id>/items/new/`
  - `/bom/<id>/enable/`
- `/bom/<id>/disable/`
- 销售：
- `/sales/orders/`
- `/sales/orders/export/`
- `/sales/orders/new/`
- `/sales/orders/<id>/`
- `/sales/orders/<id>/edit/`
- `/sales/orders/<id>/print/`
- `/sales/orders/<id>/submit/`
- `/sales/orders/<id>/confirm/`
- `/sales/orders/<id>/void/`
  - `/sales/shortages/`
  - `/sales/shortages/create-purchase-request/`
  - `/sales/shipments/`
  - `/sales/shipments/export/`
  - `/sales/shipments/<id>/`
  - `/sales/shipments/<id>/print/`
  - `/sales/shipments/<id>/confirm/`
  - `/sales/returns/`
  - `/sales/returns/<id>/`
  - `/sales/returns/<id>/confirm-receipt/`
  - `/sales/sample-loans/`
  - `/sales/sample-loans/new/`
  - `/sales/sample-loans/<id>/`
  - `/sales/sample-loans/<id>/items/new/`
  - `/sales/sample-loans/<id>/confirm-out/`
  - `/sales/sample-loans/<id>/convert-to-sales/`
  - `/sales/sample-returns/<id>/`
  - `/sales/sample-returns/<id>/confirm/`
- 采购：
  - `/purchase/requests/`
  - `/purchase/requests/new/`
  - `/purchase/requests/<id>/`
  - `/purchase/requests/<id>/edit/`
  - `/purchase/requests/<id>/void/`
  - `/purchase/requests/<id>/create-order/`
  - `/purchase/orders/`
  - `/purchase/orders/export/`
  - `/purchase/orders/new/`
  - `/purchase/orders/<id>/`
  - `/purchase/orders/<id>/edit/`
  - `/purchase/orders/<id>/void/`
  - `/purchase/orders/<id>/items/new/`
  - `/purchase/receipts/`
  - `/purchase/receipts/export/`
  - `/purchase/receipts/<id>/`
  - `/purchase/receipts/<id>/edit/`
  - `/purchase/receipts/<id>/print/`
  - `/purchase/receipts/<id>/confirm/`
  - `/purchase/supplier-returns/`
  - `/purchase/supplier-returns/<id>/`
  - `/purchase/supplier-returns/<id>/confirm-out/`
- 库存：
- `/inventory/`
- `/inventory/export/`
- `/inventory/initial-import-template/`
- `/inventory/initial-import/`
- `/inventory/locations/`
- `/inventory/batches/`
- `/inventory/batches/export/`
- `/inventory/transactions/`
- `/inventory/transactions/export/`
  - `/inventory/transfers/`
  - `/inventory/transfers/new/`
  - `/inventory/transfers/<id>/`
  - `/inventory/transfers/<id>/confirm/`
  - `/inventory/stock-counts/`
  - `/inventory/stock-counts/new/`
  - `/inventory/stock-counts/<id>/`
  - `/inventory/stock-counts/<id>/items/new/`
  - `/inventory/stock-counts/<id>/confirm/`
- 生产：
  - `/production/orders/`
  - `/production/orders/new/`
  - `/production/orders/<id>/`
  - `/production/orders/<id>/edit/`
  - `/production/orders/<id>/cancel/`
  - `/production/orders/<id>/create-requisition/`
  - `/production/orders/<id>/create-receipt/`
  - `/production/requisitions/`
  - `/production/requisitions/export/`
  - `/production/requisitions/<id>/`
  - `/production/requisitions/<id>/edit/`
  - `/production/requisitions/<id>/print/`
  - `/production/requisitions/<id>/confirm/`
  - `/production/receipts/`
  - `/production/receipts/export/`
  - `/production/receipts/<id>/`
  - `/production/receipts/<id>/edit/`
  - `/production/receipts/<id>/print/`
  - `/production/receipts/<id>/confirm/`
- 财务：
  - `/finance/customer-receipts/`
  - `/finance/customer-receipts/export/`
  - `/finance/customer-receipts/new/`
  - `/finance/customer-receipts/<id>/`
  - `/finance/customer-receipts/<id>/edit/`
  - `/finance/customer-receipts/<id>/confirm/`
  - `/finance/customer-receipts/<id>/reverse/`
  - `/finance/customer-receipts/<id>/void/`
  - `/finance/supplier-payments/`
  - `/finance/supplier-payments/export/`
  - `/finance/supplier-payments/new/`
  - `/finance/supplier-payments/<id>/`
  - `/finance/supplier-payments/<id>/edit/`
  - `/finance/supplier-payments/<id>/confirm/`
  - `/finance/supplier-payments/<id>/reverse/`
  - `/finance/supplier-payments/<id>/void/`
  - `/finance/customer-balances/`
  - `/finance/customer-balances/export/`
  - `/finance/customer-balances/<id>/`
  - `/finance/customer-balances/<id>/apply/`
  - `/finance/supplier-balances/`
  - `/finance/supplier-balances/export/`
  - `/finance/supplier-balances/<id>/`
  - `/finance/supplier-balances/<id>/apply/`
  - `/finance/reconciliations/`
  - `/finance/reconciliations/new/`
  - `/finance/reconciliations/<id>/`
  - `/finance/reconciliations/<id>/confirm/`
  - `/finance/reconciliations/<id>/void/`
- 横切模块：
  - `/approvals/`
  - `/approvals/rules/`
  - `/approvals/rules/new/`
  - `/approvals/rules/<id>/`
  - `/approvals/<id>/`
  - `/approvals/<id>/<action>/`
  - `/notifications/`
  - `/notifications/<id>/`
  - `/notifications/<id>/process/`
  - `/notifications/<id>/close/`
  - `/files/`
  - `/files/upload/`
  - `/files/<id>/`
  - `/files/<id>/download/`
- `/files/<id>/delete/`

已新增管理命令：

- `python manage.py backup_daily`
- `python manage.py verify_backups`
- `python manage.py cleanup_backups`
- `python manage.py restore_drill`
- `python manage.py process_pending_events`
- 管理员健康检查页：`/health/`
- 管理员审计日志页：`/audit-logs/`

### 2.5 已跑通的核心业务闭环

目前已通过服务层和测试跑通以下主流程：

```text
销售订单确认
  -> BOM 锁定
  -> 成品库存检查 / 子件齐套检查
  -> 欠料提醒
  -> 欠料生成采购需求
  -> 采购入库
  -> 库存增加
  -> 欠料重检为已齐套
  -> 生产领料
  -> 子件库存扣减
  -> 生产入库
  -> 成品库存增加
  -> 销售出库
  -> 成品库存扣减
  -> 销售订单发货状态更新
```

同时已覆盖：

- 盘点调整
- 期初库存导入
- 销售订单 CSV 导入
- 销售出库 CSV 导入
- 借样单 CSV 导入
- 采购需求 CSV 导入
- 采购单 CSV 导入
- 进货单 CSV 导入
- 生产领料 CSV 导入
- 生产入库 CSV 导入
- 客户收款 CSV 导入
- 供应商付款 CSV 导入
- 借样归还
- 客户收款核销
- 客户对账单收款核销
- 客户收款红冲
- 客户待处理余额处理
- 供应商付款核销
- 供应商对账单付款核销
- 供应商付款红冲
- 关键确认、红冲和余额处理动作审计日志
- 审批详情、审批动作与审批加签
- 系统消息详情、已读、处理、关闭和稍后提醒
- 附件登记、上传、下载、访问日志和逻辑删除

## 3. 当前测试覆盖

当前自动测试数量：750 个。

已覆盖测试模块：

- `sales`
- `purchase`
- `production`
- `inventory`
- `finance`
- `approvals`
- `notifications`
- `files`
- `accounts`
- `bom`
- `masterdata`
- `system`

测试重点：

- 销售订单确认后成品库存充足
- 销售订单确认后生成欠料
- 欠料生成采购需求
- 采购入库生成批次、库存汇总和库存流水
- 采购入库后欠料重检为已齐套
- 生产领料扣减库存
- 生产入库增加成品库存
- 销售出库扣减成品库存
- 盘点盘盈和盘亏
- 借样归还入库并关闭逾期
- 客户收款核销、红冲和余额处理
- 客户收款未确认编辑、作废和权限拦截
- 供应商付款核销和红冲
- 供应商付款未确认编辑、作废和权限拦截
- 客户对账单创建、详情展示、确认和作废
- 供应商对账单创建与未结金额汇总
- 对账单确认后生成固定明细快照，来源销售订单或进货单后续变化不会改变已确认对账单展示金额
- 对账单详情金额权限拦截
- 客户收款多订单核销按销售订单 ID 排序
- 供应商付款多进货单核销按进货单 ID 排序
- 客户收款可按已确认客户对账单核销，且会拦截对账单超额核销
- 供应商付款可按已确认供应商对账单核销
- 客户收款和供应商付款详情页会展示已确认对账单可核销金额和建议核销金额
- 审批通过、转交、加签、加签后返回原审批人、驳回必填原因、退回修改必填原因和提交人撤回
- 审批列表、审批详情、审批同意、转交、加签、退回修改和撤回页面动作
- 审批动作会记录 IP 和浏览器信息
- 借样出库、借样归还、借样转销售、客户退货入库、供应商退货出库和库位移库事务后事件会生成指向真实业务单据的系统消息
- 审批列表和详情页的数据范围：普通用户只能查看自己提交或当前待自己审批的审批单，权限管理员可看全量
- 审批规则列表、详情、新建和审批人必填校验
- 审批列表支持按审批单号、来源单号、来源标题、审批人和状态筛选；审批规则列表支持按单据类型、审批人/角色和状态筛选
- 系统消息读写状态、消息详情、标记已处理、关闭消息和稍后提醒
- 用户-角色-权限判断、停用用户权限失效、超级管理员权限放行
- 模块查看权限和处理权限分离：`sales.view`、`purchase.view`、`inventory.view`、`production.view`、`bom.view` 可只读进入模块，`*.process` 才能执行新增、编辑、确认、作废等处理动作
- 角色菜单隔离和直接 URL 权限拦截：销售角色只显示销售相关入口，采购角色可查看采购和欠料提醒，老板类只读角色可查看授权模块但不显示权限管理入口
- 审批规则管理无权限访问 403
- 敏感附件下载权限、附件删除权限和未授权不写下载日志
- 销售数据范围过滤和 `sales.view_all` 全量查看权限
- 工作台销售订单和欠料摘要遵守销售数据范围，普通销售只看到负责客户或自己创建的数据
- 客户与客户产品页面的数据范围过滤
- 财务金额字段脱敏和 `finance.view_amount` 金额查看权限
- 收付款处理动作权限 `finance.payment_process`
- 客户收款/供应商付款创建、红冲和客户/供应商待处理余额处理直接 POST 无完整权限会返回 403，且不会改动业务数据
- BOM 维护动作权限 `bom.process`，BOM 启用和停用要求当前密码二次验证
- 销售、采购、库存、生产关键确认类动作权限
- 生产指令手工新建要求 `production.process` 权限
- 欠料生成采购需求要求 `purchase.process` 权限
- 库存、财务和审批规则列表页的新增、导入、模板下载等按钮会按后端权限过滤
- 保存筛选的保存、设默认和删除入口会拒绝外部 `return_to` 跳转，避免开放重定向
- 物料、客户产品、供应商价格等含金额导入入口要求 `finance.view_amount`，客户、客户地址和供应商等含联系方式导入入口要求 `masterdata.view_personal_info`
- 销售订单、客户退货保存并提交时必须具备 `sales.process` 权限，普通用户只能保存草稿
- 采购需求、采购单、供应商退货保存并提交时必须具备 `purchase.process` 权限，普通用户只能保存草稿
- 销售出库待确认编辑和作废要求 `sales.process` 权限，详情页无权限时隐藏对应按钮
- 销售订单、客户退货、采购单、进货单、供应商退货的金额脱敏
- 物料最新采购价、供应商报价、客户产品默认售价的脱敏
- 无金额权限录入销售订单和采购明细时自动带入默认价格
- 客户电话、客户地址收件人和电话、供应商联系人和电话的个人信息脱敏
- 默认权限创建包含个人信息查看权限
- 附件来源单据权限过滤、无权来源上传拦截、`sales.view_all` 查看销售订单附件
- 对账单附件要求 `finance.view_amount` 来源权限，已删除附件不能重复删除或重复写删除日志
- 附件物理文件缺失时下载返回 404，且不会写入虚假的下载访问日志
- 历史导出 CSV 下载仅允许导出本人或超级管理员，非导出人的权限管理员可看日志但不能下载文件
- 历史导出 CSV 下载会拒绝 `MEDIA_ROOT/exports` 之外的数据库路径，详情页也不会显示下载入口
- 通用 CSV 导出会转义 Excel 公式样式文本，避免导出内容触发公式执行
- 各模块即时导出统一通过导出文件响应工具返回 CSV，导出相关模块测试和全量测试均通过
- 采购、生产和盘点等运营单据附件访问与详情页可见性保持一致，销售订单附件仍遵守销售数据范围
- 附件列表支持按附件号、文件名、来源类型、来源单号、上传人和状态筛选，且筛选不会绕过来源单据权限
- 销售订单详情附件面板、隐藏来源字段上传、敏感附件下载入口权限
- 事务后事件处理
- 工作台、登录页和核心列表页访问
- 侧边栏主要导航目标可访问
- 物料、客户、供应商创建和详情展示
- 物料、客户、供应商编辑和敏感字段保留
- 客户产品页面新增、客户地址页面新增和权限拦截
- 客户地址编辑要求个人信息权限，更新收件信息、状态和版本，并能按地址类型切换默认地址
- 客户产品列表、客户详情和客户产品详情页互相跳转；客户产品编辑会更新标签/包装要求、状态和版本，无金额权限时保留原默认售价
- 物料单位换算页面新增、物料供应商价格页面新增和权限拦截
- 物料单位换算编辑会更新比例、状态和版本；物料供应商价格编辑要求金额权限，更新价格时可同步切换默认供应商价格
- 供应商详情页的供应物料价格可跳转物料详情；有金额权限时可直接编辑供应商价格
- BOM 创建、详情展示、草稿头信息编辑、已启用禁止直接编辑、列表筛选、筛选后导出、复制为新版本、明细新增、明细编辑、启用后禁止删除明细、启用、停用和重复明细行校验
- 销售订单确认优先锁定默认启用 BOM
- 销售订单页面创建、草稿/驳回编辑、提交审核、详情展示、审核确认和作废
- 销售订单详情页按未发货且库存充足明细生成销售出库单
- 销售订单编辑会重算金额、重排行号、写变更日志和审计日志
- 已确认销售订单不能进入编辑页面
- 待审核销售订单作废会更新状态、写变更日志和审计日志
- 欠料生成采购需求页面动作
- 采购需求创建、详情展示、草稿/驳回编辑、待审核作废和审计日志
- 已生成下游采购单的采购需求不能编辑
- 采购需求生成采购单服务和页面动作
- 采购单创建、详情展示和明细新增
- 采购单草稿/驳回编辑、金额重算、行号重排和审计日志
- 已通过采购单不能进入编辑页面
- 待审核采购单作废会更新状态并写审计日志
- 采购单详情页按未到货数量生成进货单
- 进货单详情展示、确认入库前编辑和确认入库页面动作
- 进货单编辑会校验合格数量不能超过采购行剩余未到货数量
- 已入库进货单不能进入编辑页面
- 供应商退货创建、详情展示、草稿/驳回编辑、草稿/待审核/驳回作废和确认退货出库页面动作
- 供应商退货明细会从来源进货行自动带出物料、单价、批次和库位，并校验退货数量不能超过可退数量和批次剩余数量
- 生产领料详情展示、确认出库前编辑和确认领料出库页面动作
- 生产领料编辑会校验批次物料、库位、需求数量和批次剩余库存
- 生产入库详情展示、确认入库前编辑和确认生产入库页面动作
- 生产入库编辑会校验入库数量不能超过生产指令剩余未入库数量
- 生产指令创建、详情展示、待生产编辑、待生产取消、生成生产领料单和生成生产入库单页面动作
- 生产指令编辑会校验 BOM 成品一致、计划日期顺序，并写审计日志
- 已有领料单的生产指令不能编辑
- 销售出库详情展示、待确认编辑、待确认作废和确认出库页面动作，编辑/作废/确认均按 `sales.process` 权限控制
- 销售出库编辑会校验出库数量不能超过销售行未发货数量和批次剩余数量
- 客户退货创建、详情展示、草稿/驳回编辑、草稿/待审核/驳回作废和确认退货入库页面动作
- 客户退货明细会从来源销售行自动带出成品和单价，并校验退货数量不能超过已发货未退数量
- 盘点创建、详情展示、手工新增明细和确认调整库存页面动作
- 库位移库创建、详情展示和确认移库页面动作
- 借样创建、详情展示、明细新增、确认借样出库、借样转销售、借样归还创建、详情展示、草稿编辑、草稿/待确认作废和确认归还入库页面动作
- 借样归还明细会从借样行自动带出物料和借样单，并校验归还数量不能超过未归还且未转销售数量
- 借样归还列表支持关键词、状态筛选和 CSV 导出，且遵守销售数据范围并记录筛选条件
- 侧边栏主要导航覆盖借样归还、供应商退货、库位、客户余额、供应商余额和客户产品入口
- 客户收款详情展示和红冲页面动作
- 供应商付款详情展示和红冲页面动作
- 客户收款创建和页面核销确认动作
- 供应商付款创建和页面核销确认动作
- 客户收款详情页展示销售订单可核销金额和建议核销金额
- 供应商付款详情页展示进货单可核销金额和建议核销金额
- 每日备份生成 zip、备份记录和 SHA-256 校验值
- 备份校验发现缺失文件
- 备份命令写入后台任务记录
- 同类备份任务运行中时阻止重复启动
- `process_pending_events` 管理命令写入后台任务记录
- 后台任务失败时会通知权限管理员，事务后事件处理失败时后台任务会记录为失败
- 系统健康检查页无权限访问返回 403
- 系统健康检查和审计日志无权限访问会展示自定义 403 页面
- 不存在的地址会展示自定义 404 页面
- 权限管理员可查看系统健康检查页和最近备份/后台任务
- 系统健康检查页展示事务后事件队列概览、最近失败事件和发布门禁报告状态，失败事件可在健康页直接看到幂等键、事件类型和错误消息
- 权限管理员可查看备份记录和后台任务列表，并按关键词和状态筛选；普通用户访问返回 403
- 审计日志列表无权限访问返回 403
- 权限管理员可查看并搜索审计日志
- 销售订单确认页面动作会写入审计日志
- 旧备份清理会保留最近每日、周/月代表备份并删除过期记录和文件
- 旧备份清理命令会写入后台任务记录
- 备份恢复演练会校验数据库转储、附件文件数量和解压目录
- 备份恢复演练会拒绝包含 `../` 等路径穿越成员的 zip 文件，且不会把文件写到演练目录外
- 旧备份清理会拒绝删除备份目录外的文件路径，防止被篡改的备份记录误删服务器其他文件
- 登录页会拒绝停用、锁定和逻辑删除账号，且不会写入登录会话
- 登录用户访问系统会创建/更新 `UserSession` 记录；会话被标记为 revoked 后下一次访问会自动退出并跳转登录页
- 权限管理员可查看用户、角色、权限清单和登录会话，可维护用户与角色权限，并强制失效非当前登录会话
- 用户维护和角色维护要求当前密码与操作原因，防止误删自己的权限管理入口
- 主导航测试已覆盖用户管理、角色管理、权限清单和登录会话页面
- 恢复演练发现非法 `database.json` 会返回校验失败
- 恢复演练命令会写入后台任务记录
- 物料列表显示导出 CSV 入口
- 物料导出返回 CSV 文件并写入导出日志
- 客户导出遵守销售数据范围
- 客户产品导出遵守销售数据范围
- 供应商导出联系人无权限时脱敏
- 供应商导出联系人有权限时可见
- 物料、客户、客户产品、供应商、BOM 列表筛选后，页面结果和导出结果保持一致；客户和客户产品仍遵守销售数据范围，供应商导出仍按个人信息权限脱敏，BOM 导出会记录筛选条件
- 库存汇总导出返回 CSV 并写导出日志
- 库存批次导出返回 CSV 并写导出日志
- 库存流水导出返回 CSV 并写导出日志
- 库存汇总列表和详情页可追溯相关批次与近期库存流水
- 库存流水列表和详情页可反查批次与库存汇总
- 库存汇总列表支持物料/库位关键词搜索，筛选后导出结果和页面结果一致
- 库存流水列表支持流水号/来源单据/物料/库位/批次关键词搜索和流水类型筛选，筛选后导出结果和页面结果一致
- 库位列表支持关键词、状态筛选和 CSV 导出，筛选后导出结果和页面结果一致
- 销售订单导出返回 CSV、写导出日志、遵守销售数据范围并脱敏金额
- 欠料提醒导出返回 CSV、写导出日志并遵守销售数据范围
- 销售出库导出返回 CSV、写导出日志并遵守销售数据范围
- 客户退货导出返回 CSV、写导出日志、遵守销售数据范围并脱敏金额
- 借样导出返回 CSV、写导出日志并遵守销售数据范围
- 采购需求导出返回 CSV 并写导出日志
- 采购单导出返回 CSV、写导出日志并按权限脱敏金额
- 进货单导出返回 CSV 并写导出日志
- 供应商退货导出返回 CSV、写导出日志并按权限脱敏金额
- 库位移库导出返回 CSV 并写导出日志
- 盘点导出返回 CSV 并写导出日志
- 生产指令导出返回 CSV 并写导出日志
- 生产领料导出返回 CSV 并写导出日志
- 生产入库导出返回 CSV 并写导出日志
- 客户收款、供应商付款、客户待处理余额、供应商待处理余额和对账单导出返回 CSV、写导出日志并按权限脱敏金额
- 销售订单、借样归还、采购单、库存批次、生产领料和生产入库列表筛选后，页面结果和导出结果保持一致
- 客户收款、供应商付款、客户待处理余额和供应商待处理余额筛选后，页面结果和导出结果保持一致
- 筛选导出会在 `ExportLog.filter_json` 中记录查询条件
- 系统消息列表支持按关键词、状态和级别筛选
- 系统消息批量标记已处理和批量关闭只影响当前用户自己的消息
- 系统消息单条和批量稍后提醒会写入 `snoozed_until`
- 未到期稍后提醒消息默认从消息列表和工作台隐藏，按“稍后提醒”状态筛选时仍可查看
- 到期稍后提醒消息打开列表或工作台时会自动恢复为未读
- 销售订单打印页会写入打印日志
- 销售订单打印页金额字段会按 `finance.view_amount` 权限脱敏
- 无销售数据访问权限的用户不能访问他人销售订单打印页，也不会写入打印日志
- 销售出库打印页会写入打印日志，并按 `finance.view_amount` 权限脱敏成本价
- 客户退货打印页会写入打印日志，并按 `finance.view_amount` 权限脱敏单价和金额
- 采购单打印页会写入打印日志，并按 `finance.view_amount` 权限脱敏采购单价和金额
- 进货入库打印页会写入打印日志，并按 `finance.view_amount` 权限脱敏采购单价
- 供应商退货打印页会写入打印日志，并按 `finance.view_amount` 权限脱敏单价和金额
- 生产指令打印页会写入打印日志，并展示锁定 BOM 子件
- 生产领料打印页会写入打印日志
- 生产入库打印页会写入打印日志
- 借样打印页会写入打印日志，且无销售数据权限不能访问他人借样单
- 借样归还打印页会写入打印日志
- 库位移库打印页会写入打印日志
- 盘点打印页会写入打印日志
- 客户待处理余额打印页会写入打印日志，无金额权限时余额和处理金额脱敏
- 供应商待处理余额打印页会写入打印日志，有金额权限时显示余额和处理金额
- 对账单打印页会写入打印日志，且无金额权限访问返回 403 并不写打印日志
- 物料导入模板可以下载 CSV
- 物料 CSV 导入会创建物料并写入成功 `ImportJob`
- 物料 CSV 导入校验失败会显示错误并写入失败 `ImportJob`
- CSV 读取会拒绝超过 `ERP_MAX_CSV_IMPORT_ROWS` 的文件，并拒绝列数超过表头的畸形行；物料导入页面会显示行数超限错误且不写入业务数据
- 客户导入模板和导入页面要求个人信息权限，客户 CSV 导入会校验销售负责人账号并写 `ImportJob`
- 客户产品导入模板可以下载 CSV，客户产品 CSV 导入会校验客户编号、关联成品编码、默认售价和状态并写 `ImportJob`
- 供应商导入模板和导入页面要求个人信息权限，供应商 CSV 导入会校验重复编号和状态并写 `ImportJob`
- 物料单位换算导入模板可以下载 CSV，物料单位换算 CSV 导入会校验物料编码、单位、换算比例、重复换算和状态并写 `ImportJob`
- 客户和供应商创建页无个人信息权限时不会接收联系方式；客户地址导入模板和导入页面要求个人信息权限，客户地址 CSV 导入会校验客户编号、地址类型、默认地址、必填字段和状态并写 `ImportJob`
- 物料供应商价格导入模板和导入页面要求金额权限，物料供应商价格 CSV 导入会校验物料编码、供应商编号、采购价、日期范围、默认价格和状态并写 `ImportJob`
- 库位导入模板和导入页面要求 `inventory.process` 权限，库位 CSV 导入会校验库位编码、名称、重复编码和状态并写 `ImportJob`
- 期初库存导入模板可以下载 CSV，期初库存 CSV 导入会生成 `InitializationJob`、库存批次、库存汇总和 `initial_stock` 库存流水
- 期初库存 CSV 导入会校验物料编码、库位编码、批次号、库存类型、期初数量、成本单价和入库日期
- 期初库存导入和模板下载要求 `inventory.process` 权限
- 销售订单导入模板和导入页面要求 `sales.process` 权限，销售订单 CSV 导入会校验客户、客户产品、日期、数量和重复明细，并生成草稿订单写入 `ImportJob`
- 销售出库导入模板和导入页面要求 `sales.process` 权限，销售出库 CSV 导入会校验销售数据范围、来源销售订单行、出库日期、未发货数量、批次和库位，并生成待确认出库单写入 `ImportJob`
- 借样单导入模板和导入页面要求 `sales.process` 权限，借样单 CSV 导入会校验客户、成品物料、借出日期、预计归还日期、借出数量、批次和库位，并生成待审核借样单写入 `ImportJob`
- 采购需求导入模板和导入页面要求 `purchase.process` 权限，采购需求 CSV 导入会校验物料、建议供应商、需求日期、数量和重复明细，并生成草稿采购需求写入 `ImportJob`
- 采购单导入模板和导入页面要求 `purchase.process` 权限，采购单 CSV 导入会校验供应商、物料、采购日期、数量、单价和重复明细；有金额权限时使用 CSV 单价，无金额权限时使用默认采购价，并生成草稿采购单写入 `ImportJob`
- 进货单导入模板和导入页面要求 `purchase.process` 权限，进货单 CSV 导入会校验来源采购单、来源采购行、进货日期、到货/合格/不合格数量和入库库位，并生成待入库进货单写入 `ImportJob`
- 生产领料导入模板和导入页面要求 `production.process` 权限，生产领料 CSV 导入会校验生产指令、锁定 BOM 子件、领料日期、需求/实领数量、批次和库位，并生成待确认领料单写入 `ImportJob`
- 生产入库导入模板和导入页面要求 `production.process` 权限，生产入库 CSV 导入会校验生产指令状态、入库日期、剩余未入库数量、库位、批次号和质量状态，并生成待确认入库单写入 `ImportJob`
- 客户收款和供应商付款导入模板和导入页面要求 `finance.view_amount` 与 `finance.payment_process` 权限，CSV 导入会校验客户/供应商、日期、金额和收付款方式，并生成待审核收付款单写入 `ImportJob`
- 主数据、库位、期初库存、销售订单、销售出库、借样单、客户退货、采购需求、采购单、进货单、供应商退货、生产指令、生产领料、生产入库、客户收款和供应商付款 CSV 导入会在解析前拒绝超过统一大小或行数上限的文件，避免误上传大文件造成内存压力；畸形 CSV 会返回业务错误并写入导入任务
- 导入任务、初始化任务、导出日志和打印日志列表要求权限管理员访问，并可按关键词或状态筛选；导入任务和初始化任务详情页会展示行号、字段和错误消息；导出日志详情页会展示筛选条件并可下载历史导出 CSV，文件缺失时返回 404；打印日志详情页会展示来源单据和打印人
- 库位列表、详情、新建和编辑页面；库位新建和编辑要求 `inventory.process` 权限
- 库存汇总详情页展示相关批次和近期库存流水；库存流水详情页展示来源信息并可反查批次和库存汇总
- 客户待处理余额详情展示和余额处理页面动作
- 供应商待处理余额详情展示和余额处理页面动作
- 附件类型校验、上传保存、详情展示、下载写访问日志、逻辑删除、已删除附件不可下载
- 附件登记和下载会拒绝 `attachments/` 目录外路径，路径异常时不会写入虚假下载日志
- 生产配置导入测试会验证 DEBUG 误开和 PostgreSQL 密码为空时应用拒绝启动

## 4. 当前尚未完成内容

### 4.1 页面与路由

当前已完成页面骨架、核心列表入口，以及主链路、日常运营关键确认动作、多数常用维护页面和主要导入/导出/打印/运维入口。主导航已经按角色权限隔离，直接访问 URL 也会做后端权限拦截。整体已接近可试运行状态。

尚未完成：

- 少量边缘单据或配置类页面的编辑、关闭、作废流程仍可继续细化
- 更多模块的数据范围规则仍可继续补强；当前销售数据范围、财务金额、个人信息、附件来源权限和模块级页面访问已完成主要保护
- 页面级错误提示、批量操作反馈和空状态提示仍可继续打磨
- 高级排序和更多组合筛选尚未系统化落地

### 4.2 权限落地

权限底座已经接入，当前已完成用户-角色、角色-权限关系、默认权限码、用户/角色维护页面、登录会话强制失效、审批规则管理权限、敏感附件权限、附件来源单据权限、销售数据范围、个人信息脱敏、销售/采购/库存/生产/财务关键动作权限、模块查看权限、主导航按角色隔离、列表/详情/导出/打印 URL 后端权限保护、财务和非财务高风险动作当前密码二次验证，以及销售、采购、财务和主数据价格金额脱敏。仍需继续扩展：

- 更细的页面按钮可见性和空状态提示
- 采购、生产、库存等更多模块的数据范围规则

### 4.3 Excel、打印、备份和后台命令

尚未完成：

- 业务单据类导入模板已覆盖主数据、库位、期初库存、销售订单、销售出库、借样单、客户退货、采购需求、采购单、进货单、供应商退货、生产指令、生产领料、生产入库、客户收款和供应商付款；模板首行已改为中文表头，导入时不再兼容旧英文模板；后续主要是按真实样表继续调整字段顺序和提示文案
- 打印模板仍可继续做版式细化和真实纸张适配；当前主要业务打印页与打印日志已接入
- 期初库存导入已支持预览确认、当前密码二次验证和未使用批次撤销，撤销必须填写原因并写入审计日志
- 后台任务已有备份、校验、清理、恢复演练、事务后事件处理和生产上线预检命令；失败任务会主动通知权限管理员，健康检查页可查看失败事件队列，生产部署与任务调度已有独立运维手册，后续只需按实际服务器路径微调
- 日志轮转已使用 Windows 友好的安全轮转 handler，短暂文件占用不会中断请求或测试输出

### 4.4 API 与前端交互

尚未完成：

- REST API 暂未建设；当前以 Django 页面和业务 POST action 为主
- 前端仍是朴素服务端模板，未做复杂交互组件和移动端专项优化
- 更多列表排序和高级筛选仍可继续补充
- 更多边缘业务单据或基础资料的附件入口仍可继续补充

### 4.5 服务层仍需增强的地方

当前服务层已覆盖 MVP 主逻辑，但以下能力后续应增强：

- 权限校验已在部分页面入口接入，服务层统一权限校验仍需继续补强
- 审计日志已覆盖关键页面动作，服务层内部自动审计和更多边缘动作后续可继续补强
- 对账单确认已建立固定对账明细快照；核销仍按对账单头金额控制余额，后续如需更细可扩展到明细级核销
- `process_pending_events` 已有稳定事件分发和单据级消息追踪，失败事件会落后台任务失败告警；各事件的业务副作用后续还可继续扩展
- 销售订单头状态、收付款状态、生产状态后续可继续细化
- 附件页面已有敏感附件权限和来源单据权限，后续可继续优化为更细的单据类型权限
- 附件病毒扫描已提供外部命令钩子，后续上线时只需按实际服务器杀毒引擎配置 `ERP_ATTACHMENT_SCAN_COMMAND`
- 导入日志已接入物料、物料单位换算、物料供应商价格、客户、客户地址、客户产品、供应商、库位、销售订单、销售出库、借样单、客户退货、采购需求、采购单、进货单、供应商退货、生产指令、生产领料、生产入库、客户收款和供应商付款 CSV 导入；初始化任务已接入期初库存 CSV 导入；导出日志和销售订单、销售出库、客户退货、借样、借样归还、采购单、进货入库、供应商退货、库位移库、盘点、生产指令、生产领料、生产入库、客户收款、供应商付款、客户待处理余额、供应商待处理余额、对账单打印日志已接入业务逻辑，更多业务导入类型和打印模板待补齐

## 5. 建议下一步开发顺序

### 第一步：继续落销售到出库主链路页面

已完成：

1. 客户列表 / 客户详情 / 客户创建
2. 物料列表 / 物料详情 / 物料创建
3. BOM 列表 / BOM 详情 / BOM 创建 / BOM 明细新增 / BOM 启用停用
4. 销售订单列表 / 详情 / 创建
5. 销售订单确认动作
6. 欠料列表
7. 采购需求生成动作
8. 采购入库确认动作
9. 生产领料确认动作
10. 生产入库确认动作
11. 销售出库待确认编辑、作废和确认动作
12. 盘点单创建和确认调整动作
13. 借样单创建和明细维护
14. 借样出库确认动作
15. 供应商退货创建、编辑、作废和确认出库动作
16. 库位移库创建和确认动作
17. 借样转销售动作
18. 客户退货创建、编辑、作废和确认入库动作

目标：把已经跑通的服务层主链路变成用户可操作流程。当前销售订单到销售出库的 MVP 主链路页面已贯通。

### 第二步：落库存、借样和财务页面

建议继续：

1. 更多业务创建 / 编辑页面
2. 更多边缘单据的关闭、作废和变更记录
3. 更多高级筛选和排序
4. 更多导入模板处理
5. 对账单固定明细快照表和更强的底层订单级防重复核销

目标：补齐日常运营需要的操作入口。

### 第三步：审批、消息、附件

建议继续：

1. 更多按钮级权限控制
2. 边缘单据或基础资料的附件入口
3. 异常处理页面和操作失败提示优化

目标：让横切能力真正服务业务页面。

### 第四步：导入、导出、打印、备份

建议最后做：

1. 更多业务单据导入模板处理
2. 打印模板真实纸张版式细化
3. 按实际服务器路径复核上线运维手册
4. 期初库存导入的预览确认和撤销能力

目标：补齐上线和运维能力。

## 6. 下一阶段测试策略

页面开发阶段建议继续保持以下测试策略：

- 每接一个 service 到 view，至少补一个 view 测试
- 每个关键 POST 动作测试：
  - 正常提交
  - 状态不允许
  - 幂等重复提交
  - 库存不足或核销超额
- 权限测试单独补：
  - 未登录不能访问
  - 无权限不能操作
  - 非负责人看不到不属于自己的数据
- 附件测试单独补：
  - 不允许类型拒绝上传
  - 已删除附件不能下载
  - 下载写访问日志

每完成一个阶段继续执行：

```powershell
.\.venv\Scripts\python manage.py check
.\.venv\Scripts\python manage.py test
```

## 7. 当前结论

当前 ERP 项目已经完成“数据模型 + 核心服务层 + 页面骨架 + 主要业务页面 + 角色权限隔离 + 中文导入模板 + 导入导出打印 + Windows 内网安装包 + 运维入口 + 自动测试”的主体基础。

目前已具备进入内部试运行/预上线环境验证的基础，主要差距在真实业务数据压测、打印版式精修、少量边缘页面体验和生产服务器实机演练。

下一步应优先做真实服务器部署演练、初始化数据试导入、各岗位账号试用和端到端业务试跑。

## 8. 2026-06-12 稳定版本交付状态

本轮已完成上线前自动化门禁复验，并额外补齐 PostgreSQL 真实行锁并发验证。当前结论是：代码层面无上线阻塞项，可以进入生产部署和期初数据准备阶段。

### 8.1 已通过的自动化证据

| 检查项 | 结果 |
| --- | --- |
| `python manage.py release_gate --include-tests --report-file docs/latest-release-gate-report.md` | 通过，13/13 |
| 完整自动测试 | 通过，750 tests OK |
| PostgreSQL 行锁并发测试 | 通过，3 tests OK |
| URL 引用完整性检查 | 通过，678 个静态引用、682 个 URL 名称、279 个模板参数引用 |
| 模板语法检查 | 通过，105 个模板 |
| 权限配置检查 | 通过，17 个默认权限 |
| 权限引用完整性检查 | 通过，15 个静态权限引用、17 个默认权限 |
| 路由保护检查 | 通过，290 个业务 URL 均受登录或权限保护 |
| CSRF 表单检查 | 通过，100 个 POST 表单均包含 csrf_token |
| 导航页面烟测 | 通过，48 个主导航页面可打开 |
| 低频入口烟测 | 通过，91 个入口可反转或访问 |
| 迁移一致性 | 通过，No changes detected |
| 业务冒烟测试 | 通过，临时业务数据已回滚 |

PostgreSQL 本机测试环境已安装并验证：

- PostgreSQL 版本：17.10。
- 本机服务：`postgresql-x64-17`，状态 Running。
- 测试数据库：`erp_test`。
- 应用测试账号：`erp_app`。

### 8.2 当前版本范围

当前稳定版本覆盖：

- 12 个 Django App 与核心数据模型。
- 销售、采购、库存、BOM、生产、借样、退货、财务、审批、消息、附件、系统运维的 MVP 主流程。
- 核心服务层事务、幂等、库存批次与汇总一致性、欠料生命周期、收付款核销和红冲。
- 主要业务页面、低频入口、导入导出、打印日志、健康检查、备份、恢复演练、发布门禁和预上线报告。
- 安全测试、权限测试、端到端业务流程测试、边界数据测试、导入导出测试、运维测试、PostgreSQL 并发测试。

### 8.3 上线前仍需人工或环境完成

这些事项不是代码阻塞，但必须在正式服务器上完成并留痕：

1. 配置生产 `.env`：`DJANGO_ENV=production`、`DJANGO_SECRET_KEY`、`DJANGO_ALLOWED_HOSTS`、`POSTGRES_*`、HTTPS 与目录路径。
2. 使用真实 PostgreSQL 空库做一次部署演练：`migrate`、`collectstatic`、`bootstrap_admin`、`release_gate`、`prelaunch_report`。
3. 配置附件扫描命令；如暂不启用，填写 `ERP_ATTACHMENT_SCAN_RISK_ACCEPTED_BY` 并在验收报告中记录风险接受人。
4. 配置每日备份、备份校验、恢复演练、事务后事件处理等计划任务。
5. 准备并试导入期初数据：客户、供应商、物料、客户产品、单位换算、库位、BOM、期初库存。
6. 业务负责人核对期初库存、应收应付、关键单据编号和打印样式。
7. 在生产环境重新执行完整上线门禁和预上线报告，报告必须无失败项。

### 8.4 下一步推荐动作

推荐下一步按以下顺序执行：

1. 将当前稳定代码提交到本地 Git，排除 `.tmp/` 中的 PostgreSQL 安装包。
2. 在服务器或准生产目录按 `docs/生产上线部署与运维手册.md` 执行空库部署演练。
3. 生成并归档本次部署命令清单、发布门禁报告和预上线验收报告。
4. 进行期初数据试导入和业务方核对。
5. 核对无误后执行正式导入，进入内部试运行。

## 9. 2026-06-13 安装包与角色权限交付状态

本轮围绕现场部署和岗位隔离继续补强，目标是让普通文员能按手册完成 Windows 10 内网安装，并让销售、采购、仓管、生产、财务、老板等角色登录后只看到自己应看到的模块。

### 9.1 已完成改动

| 类别 | 完成内容 |
| --- | --- |
| 模块权限 | 新增 `sales.view`、`purchase.view`、`inventory.view`、`production.view`、`bom.view` 五个模块查看权限；默认权限总数变为 17 个 |
| 角色菜单 | 主导航按权限显示模块；销售角色只显示销售相关入口，采购角色可查看采购和欠料提醒，老板类只读角色可查看授权模块但不显示权限管理入口 |
| 后端拦截 | 销售、采购、库存、生产、BOM、财务主要列表、详情、导出和打印入口均做后端权限校验，防止直接输入 URL 绕过菜单 |
| 财务隔离 | 财务页面继续要求 `finance.view_amount`，收付款确认、红冲、待处理余额处理继续叠加 `finance.payment_process` |
| 中文化 | 权限清单、用户详情、角色详情和常见表单字段改为中文展示；系统通用码值显示增加中文映射 |
| 导入模板 | CSV 导入模板首行改为中文表头；导入解析只接受系统中文表头，不再兼容旧英文模板，避免普通用户看不懂模板 |
| 安装器 | `ERP-Setup.exe` 增加启动日志和错误提示；控制台启动器避免中文批处理乱码；安装日志固定写入 `installer\logs` |
| 离线依赖 | Python 依赖 wheel 已放入 `installer\wheels`，安装时优先离线安装，不依赖现场服务器联网 |
| Windows 服务 | 安装包内置 `installer\tools\nssm.exe`，注册 Waitress 服务时自动使用；服务启动后自动放行 TCP 8000 防火墙端口 |
| 安装包 | `ERP安装包` 排除 `tests_safety`、开发期大安装包目录和 `ERP模块建设计划.md`；保留一键安装手册、运行代码、离线 wheels、NSSM 和最新发布门禁报告 |

### 9.2 最新验证结果

| 检查项 | 结果 |
| --- | --- |
| 完整自动测试 | 通过，762 tests OK，3 个 SQLite 不支持的 PostgreSQL 行锁并发用例按预期跳过 |
| 发布前门禁 | 通过，13/13；报告已更新到 `docs/latest-release-gate-report.md` |
| 权限配置检查 | 通过，17 个默认权限 |
| 权限引用检查 | 通过，15 个静态权限引用、17 个默认权限 |
| URL 引用检查 | 通过，715 个静态引用、712 个 URL 名称、294 个模板参数引用 |
| 模板语法检查 | 通过，113 个模板 |
| 安装包构建 | 通过，已重新生成 `ERP安装包` 和 `ERP安装包.zip`；构建脚本已兼容 Windows PowerShell 中文路径解析 |

### 9.3 下一步建议

1. 用最新 `ERP安装包.zip` 在一台干净 Windows 10 内网电脑上做完整重装演练。
2. 分别创建销售、采购、仓管、生产、财务、老板和管理员账号，逐个登录确认菜单、直接 URL、导出、打印和动作权限。
3. 用中文模板导入少量真实样本数据，确认中文、英文和数字混合录入不会影响数据库保存和查询。
4. 在现场服务器完成期初数据试导入、业务负责人核对、备份恢复演练和内部试运行。

## 10. 2026-06-26 业务易用性与财务建账改进

本轮根据现场使用建议继续做第一阶段优化，重点降低日常录入难度，并补齐上线期初建账和管理费用入口。

### 10.1 已完成改动

| 类别 | 完成内容 |
| --- | --- |
| 期初库存 | 新增“手工期初建账”入口，文员可在页面逐条录入物料、库位、批次、数量和成本，不必必须整理 CSV；仍复用初始化任务预览、确认和撤销流程 |
| 期初应收 | 新增期初应收列表、新建、详情和作废；客户收款确认时可直接核销期初应收，红冲收款会自动回滚期初应收余额 |
| 期初应付 | 新增期初应付列表、新建、详情和作废；供应商付款确认时可直接核销期初应付，红冲付款会自动回滚期初应付余额 |
| 管理费用 | 新增管理费用登记、详情、确认和作废，支持采购辅料、运费、电费、房租、设备采购、餐费、业务费用、礼品、办公费用等类别 |
| 销售订单 | 销售订单明细行已支持页面动态新增和删除；订单头新增客户合同编号和结算方式 |
| 送货单 | 送货单打印改为不显示价格，增加公司抬头、地址、电话、联系人、客户合同号、客户地址、客户联系人、客户电话、结算方式和备注，并按 3 联 2 等分纸张方向初步适配 |
| 客户产品 | 从主导航隐藏独立“客户产品”入口，降低普通销售/文员菜单复杂度；底层资料和客户详情内维护入口保留，避免影响历史订单、BOM 锁定和导入逻辑 |
| 产品组成清单 | 基础资料中的 BOM 主入口已改为“产品组成清单”，页面文案改为原料/配件组成、清单编号、默认清单等普通业务说法；底层模型、URL 和历史数据仍保留 BOM 结构 |
| 经营看板 | 财务模块新增“经营看板”，按本月/本年展示销售额、采购入库额、已收款、已付款、管理费用、现金净额、估算经营额、当前应收应付和待处理余额 |
| CSV 兼容 | 上传 CSV 支持 UTF-8 BOM 和 GB18030/GBK 文本，日期支持 `YYYY/M/D` 等斜杠格式；导入模板仍以中文表头为准 |
| 中文化 | 新增期初应收、期初应付、管理费用相关字段、审计动作和列表显示均已补中文映射 |

### 10.2 新增数据库与迁移

- 新增 `finance.OpeningReceivable`：期初应收。
- 新增 `finance.OpeningPayable`：期初应付。
- 新增 `finance.ExpenseRecord`：管理费用。
- `CustomerReceiptAllocation` 新增 `opening_receivable` 外键，可核销期初应收。
- `SupplierPaymentAllocation` 新增 `opening_payable` 外键，可核销期初应付。
- 已生成并应用 `finance.0003_openingreceivable_and_more`。
- 已生成并应用 `sales.0003_salesorder_customer_contract_no_and_more`。

### 10.3 已验证结果

| 检查项 | 结果 |
| --- | --- |
| `python manage.py check` | 通过 |
| `python manage.py makemigrations --check` | 通过，No changes detected |
| `python manage.py migrate` | 通过，本地迁移已应用 |
| `python manage.py check_templates` | 通过，113 个模板 |
| `python manage.py check_url_references` | 通过，715 个静态引用、712 个 URL 名称、294 个模板参数引用 |
| `python manage.py check_route_protection` | 通过，305 个业务 URL 均有登录或权限保护 |
| `python manage.py check_csrf_tokens` | 通过，108 个 POST 表单均包含 csrf_token |
| `python manage.py check_navigation_pages` | 通过，51 个主导航页面可打开 |
| `python manage.py test bom finance system.tests.SystemDashboardTests system.tests.ReleaseGateCommandTests` | 通过，152 tests OK |
| `python manage.py test finance` | 通过，74 tests OK |
| `python manage.py release_gate --operator admin --include-tests --report-file docs\latest-release-gate-report.md` | 通过，13/13；完整自动测试 762 tests OK |
| `powershell -ExecutionPolicy Bypass -File .\installer\build-install-package.ps1` | 通过，已重建 `ERP安装包` 并校验离线 wheels、NSSM、新迁移和新增页面均在包内 |

### 10.4 下一步建议

1. 第一阶段已执行：固化当前可用版本，完成全量门禁、安装包重建、离线依赖和安装包内容校验。
2. 第二阶段建议：结合真实送货单样张继续微调打印版式，尤其是 3 联 2 等分纸张的字号、行高、签收栏和公司信息位置。
3. 第三阶段建议：继续完善经营看板，后续接入生产成本、库存成本和费用分摊后，再升级为正式利润分析。
4. 第四阶段建议：在现场用销售、采购、仓管、生产、财务、老板账号做一轮真实业务试运行，并补期初应收/应付 CSV 导入模板。
