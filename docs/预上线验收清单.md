# ERP 预上线验收清单

生成时间：2026-06-11

本文档用于正式上线前的最后验收。每一项都应由执行人填写结果、时间和证据位置；未通过项必须记录处理结论后再进入生产。

## 1. 代码与依赖门禁

| 检查项 | 命令或动作 | 通过标准 | 结果 |
| --- | --- | --- | --- |
| Django 系统检查 | `python manage.py check` | 无错误 | 待执行 |
| URL 引用完整性 | `python manage.py check_url_references` | 模板和 Python 代码中的静态 URL name、模板静态 URL 参数全部可解析 | 待执行 |
| 模板语法检查 | `python manage.py check_templates` | 所有 Django 模板均可编译 | 待执行 |
| 权限配置检查 | `python manage.py check_permissions` | 代码权限码、默认权限清单和数据库默认权限记录一致 | 待执行 |
| 权限引用完整性 | `python manage.py check_permission_references` | 代码和模板中的静态权限码引用全部已登记为默认权限 | 待执行 |
| 路由保护检查 | `python manage.py check_route_protection` | 除登录/退出和 Django Admin 外，业务 URL 均继承登录或 ERP 权限保护 | 待执行 |
| CSRF 表单检查 | `python manage.py check_csrf_tokens` | 模板中的 POST 表单均包含 `csrf_token` | 待执行 |
| 导航页面烟测 | `python manage.py check_navigation_pages --username <管理员用户名>` | 主导航页面全部返回 200，无 403、404 或 500 | 待执行 |
| 低频入口烟测 | `python manage.py check_low_frequency_entrypoints --username <管理员用户名>` | 导入、导出、打印入口均可反转；导入页和导入模板入口可访问 | 待执行 |
| 迁移一致性 | `python manage.py makemigrations --check --dry-run` | `No changes detected` | 待执行 |
| 自动测试 | `python manage.py test --noinput --verbosity 1` | 全部测试 OK | 待执行 |
| 依赖完整性 | `python -m pip check` | `No broken requirements found` | 待执行 |
| 生产配置模拟 | `python manage.py simulate_production_settings --host <正式域名>` | 生产环境变量组合可正常导入 settings，`check --deploy` 无警告 | 待执行 |
| 部署命令清单 | `python manage.py deployment_runbook --host <正式域名> --operator <用户名> --release-version <版本号> --output-file docs/deployment-runbook-<版本号>.md` | 输出命令顺序已归档，现场执行人与版本号一致 | 待执行 |
| 生产安全检查 | 设置生产环境变量后执行 `python manage.py check --deploy` | 无警告或仅有已记录的风险接受项 | 待执行 |
| 业务冒烟测试 | `python manage.py business_smoke_test --operator <用户名>` | 销售出库、欠料采购、生产领料、生产入库、借样出库、借样归还、借样转销售、收付款核销、待处理余额和红冲链路通过，默认回滚临时数据 | 待执行 |
| 发布前总门禁 | `python manage.py release_gate --operator <用户名> --include-deploy-check --include-tests --include-production-preflight --report-file docs/latest-release-gate-report.md` | 系统检查、生产安全检查、URL 引用完整性检查、模板语法检查、权限配置检查、权限引用完整性检查、路由保护检查、CSRF 表单检查、导航页面烟测、低频入口烟测、迁移一致性、依赖检查、业务冒烟、自动测试和严格生产预检全部通过，并生成门禁报告 | 待执行 |
| 发布记录 | `python manage.py record_release <版本号> --summary "<发布摘要>" --released-by <用户名>` | 系统内生成唯一发布记录 | 待执行 |
| 预上线验收报告 | `python manage.py prelaunch_report --strict --bootstrap-username <用户名> --release-version <版本号> --report-file docs/prelaunch-acceptance-report.md --deployment-runbook-file docs/deployment-runbook-<版本号>.md` | 发布门禁、初始化验收、生产预检、部署命令清单、新鲜成功备份、新鲜备份校验、新鲜恢复演练、最近事务后事件处理、后台任务和本次发布记录汇总为可归档报告，且无失败/预警 | 待执行 |

## 2. 生产配置门禁

| 检查项 | 通过标准 | 结果 |
| --- | --- | --- |
| 生产标记 | `.env` 中 `DJANGO_ENV=production` | 待执行 |
| 调试开关 | `DJANGO_DEBUG=false` | 待执行 |
| 密钥 | `DJANGO_SECRET_KEY` 为高强度随机值，且不是示例值 | 待执行 |
| 主机名 | `DJANGO_ALLOWED_HOSTS` 只包含正式域名或内网正式访问地址 | 待执行 |
| 数据库 | `DB_ENGINE=postgres`，应用不连接 SQLite | 待执行 |
| 数据库密码 | `POSTGRES_PASSWORD` 已填写，且不是默认示例值 | 待执行 |
| HTTPS | Secure Cookie、CSRF Trusted Origins、SSL Redirect、HSTS 按部署方式配置 | 待执行 |
| 目录隔离 | 附件、备份、静态文件目录互相隔离 | 待执行 |
| 附件扫描 | 已配置 `ERP_ATTACHMENT_SCAN_COMMAND`；未配置时已填写 `ERP_ATTACHMENT_SCAN_RISK_ACCEPTED_BY` 并在报告中显示风险接受人 | 待执行 |
| 自动预检 | `python manage.py production_preflight --strict` 通过 | 待执行 |

## 3. 初始化验收

| 检查项 | 通过标准 | 结果 |
| --- | --- | --- |
| 数据库迁移 | `python manage.py migrate` 已在生产库执行成功 | 待执行 |
| 静态文件 | `python manage.py collectstatic --noinput` 已执行，反向代理可访问静态资源 | 待执行 |
| 初始管理员 | `python manage.py bootstrap_admin ... --noinput` 已执行，且 `python manage.py bootstrap_admin --username <用户名> --check-only` 通过 | 待执行 |
| 权限管理员 | 至少一个启用用户拥有权限管理角色 | 待执行 |
| 业务角色 | 销售、采购、仓库、生产、财务、审批等角色已配置 | 待执行 |
| 审批规则 | BOM、销售订单、采购、收付款等关键审批规则已配置或明确关闭 | 待执行 |
| 基础资料 | 客户、供应商、物料、客户产品、单位换算、库位已导入并抽查 | 待执行 |
| 期初库存 | 期初库存导入成功，库存汇总与抽查批次一致 | 待执行 |

## 4. 主业务流程试跑

| 流程 | 必测动作 | 通过标准 | 结果 |
| --- | --- | --- | --- |
| 销售订单 | 新建、提交、确认、库存检查 | 库存充足和欠料两种路径都正确 | 待执行 |
| 欠料采购 | 欠料生成采购需求、采购单、进货单、确认入库 | 入库后欠料可重新检查并变为已齐套 | 待执行 |
| 生产 | 创建生产指令、生产领料、生产入库 | 原料库存扣减、成品库存增加，批次可追溯 | 待执行 |
| 销售出库 | 创建出库单、确认出库、打印 | 成品库存扣减，销售订单发货数量更新 | 待执行 |
| 借样 | 借样出库、部分归还、转销售 | 样品库存和借样状态正确 | 待执行 |
| 退货 | 客户退货入库、供应商退货出库 | 库存和应收应付边界正确 | 待执行 |
| 盘点 | 创建盘点单、录入差异、确认调整 | 生成库存流水并保持批次与汇总一致 | 待执行 |
| 收付款 | 客户收款、供应商付款、多单核销、红冲 | 不允许超额核销，红冲后余额正确 | 待执行 |
| 对账 | 生成对账单、确认、打印 | 已确认对账单使用固定明细快照 | 待执行 |

## 5. 权限与审计验收

| 检查项 | 通过标准 | 结果 |
| --- | --- | --- |
| 登录限制 | 停用、锁定、逻辑删除用户无法登录 | 待执行 |
| Admin 限制 | 普通 staff 无法进入 `/admin/` | 待执行 |
| 数据范围 | 普通销售只能看到自己负责的数据 | 待执行 |
| 金额脱敏 | 无金额权限用户看不到销售、采购、财务金额 | 待执行 |
| 个人信息脱敏 | 无个人信息权限用户看不到联系人、电话、地址 | 待执行 |
| 附件权限 | 敏感附件下载需权限，下载写访问日志 | 待执行 |
| 二次验证 | 高风险动作要求当前密码和操作原因 | 待执行 |
| 审计日志 | 关键新增、编辑、确认、作废、删除动作都有审计记录 | 待执行 |
| 会话管理 | 管理员可强制失效其他用户会话 | 待执行 |

## 6. 导入、导出与打印验收

| 检查项 | 通过标准 | 结果 |
| --- | --- | --- |
| 导入模板 | 已下载并核对主数据、销售、采购、库存、生产、财务模板 | 待执行 |
| 导入错误 | 错误 CSV 不写入业务数据，导入任务展示行号和错误 | 待执行 |
| 导出安全 | 导出 CSV 防公式注入，历史导出只能下载合法导出文件 | 待执行 |
| 打印日志 | 主要单据打印会写打印日志 | 待执行 |
| 打印脱敏 | 无金额权限时打印页金额脱敏 | 待执行 |

## 7. 运维与恢复验收

| 检查项 | 命令或动作 | 通过标准 | 结果 |
| --- | --- | --- | --- |
| 健康检查 | 访问 `/health/` | 数据库、目录、后台任务状态可见，无超时后台任务和超时事务后事件，最近发布门禁报告显示通过且未过期 | 待执行 |
| 每日备份 | `python manage.py backup_daily` | 生成备份记录和 zip 文件 | 待执行 |
| 备份校验 | `python manage.py verify_backups` | 最近备份校验通过 | 待执行 |
| 恢复演练 | `python manage.py restore_drill` | 可解压并校验数据库转储和附件 | 待执行 |
| 备份清理 | `python manage.py cleanup_backups` | 只清理备份目录内过期备份 | 待执行 |
| 事务后事件 | `python manage.py process_pending_events` | 无待处理、处理中、超时处理中或失败事件积压，预上线报告显示最近事件处理任务未过期 | 待执行 |
| 发布记录查询 | 访问 `/release-records/` | 可查看本次发布版本、发布人和摘要 | 待执行 |
| 后台任务告警 | 人为制造失败任务后通知权限管理员 | 可收到紧急系统消息 | 待执行 |
| 日志轮转 | 检查 `DJANGO_LOG_DIR` | 日志可写且轮转不报错 | 待执行 |

## 8. 上线签字

| 角色 | 姓名 | 结论 | 日期 |
| --- | --- | --- | --- |
| 业务负责人 |  |  |  |
| 财务负责人 |  |  |  |
| 仓库负责人 |  |  |  |
| 系统管理员 |  |  |  |
| 开发负责人 |  |  |  |

上线结论：

- [ ] 允许上线
- [ ] 延期上线，原因：
